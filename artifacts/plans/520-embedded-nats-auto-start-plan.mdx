---
title: Replace multibot with embedded NATS auto-start
issue: 520
status: approved
tier: F-lite
date: 2026-04-04
spec: artifacts/specs/520-embedded-nats-auto-start-spec.mdx
---

## Overview

4 slices, 19 tasks. Slices 1 and 2 are fully parallel (no shared files). Slice 3 depends on both. Slice 4 depends on Slice 3.

```
Slice 1 (embedded_nats)  ──┐
                            ├──▶ Slice 3 (unified bootstrap) ──▶ Slice 4 (cleanup + docs)
Slice 2 (rename modules) ──┘
```

## Slice 1 — Embedded NATS manager

**Agent:** `backend-dev`
**Parallel with:** Slice 2

| # | Task | File(s) | Action |
|---|------|---------|--------|
| 1.1 | Create `EmbeddedNats` class | `src/lyra/bootstrap/embedded_nats.py` (new) | `start()` — `shutil.which("nats-server")`, spawn via `asyncio.create_subprocess_exec` with `-a 127.0.0.1 -p {port} --no_auth`, register `atexit` handler |
| 1.2 | Implement `wait_ready()` | same file | TCP poll `127.0.0.1:{port}` every 0.1s, timeout raises `RuntimeError` with port-in-use message |
| 1.3 | Implement `stop()` | same file | `process.terminate()`, 3s wait, `process.kill()` fallback, deregister atexit |
| 1.4 | Write unit tests | `tests/bootstrap/test_embedded_nats.py` (new) | Test: binary found/not-found, start/stop lifecycle, wait_ready timeout, atexit registration |

**RED-GATE after 1.4:** `pytest tests/bootstrap/test_embedded_nats.py` must pass.

## Slice 2 — Rename shared modules

**Agent:** `backend-dev`
**Parallel with:** Slice 1

| # | Task | File(s) | Action |
|---|------|---------|--------|
| 2.1 | Rename `multibot_stores.py` → `bootstrap_stores.py` | `src/lyra/bootstrap/` | `git mv`, update all imports |
| 2.2 | Rename `multibot_wiring.py` → `bootstrap_wiring.py` | `src/lyra/bootstrap/` | `git mv`, keep `_build_bot_auths` only, delete `wire_telegram_adapters` / `wire_discord_adapters` |
| 2.3 | Update import sites | `hub_standalone.py`, `tests/conftest.py`, `tests/test_main_auth.py`, `tests/test_bootstrap_credential_resolution.py` | Fix imports to new module names |
| 2.4 | Rename test files | `tests/bootstrap/test_multibot_stores.py` → `test_bootstrap_stores.py`, `tests/bootstrap/test_multibot_wiring.py` → `test_bootstrap_wiring.py` | `git mv` + fix internal imports |

**RED-GATE after 2.4:** `pytest tests/bootstrap/test_bootstrap_stores.py tests/bootstrap/test_bootstrap_wiring.py tests/test_main_auth.py tests/test_bootstrap_credential_resolution.py` — all pass, no `multibot_stores` or `multibot_wiring` imports remain.

## Slice 3 — Unified bootstrap

**Agent:** `backend-dev`
**Depends on:** Slices 1 + 2

| # | Task | File(s) | Action |
|---|------|---------|--------|
| 3.1 | Create `_bootstrap_unified()` | `src/lyra/bootstrap/unified.py` (new) | NATS_URL check → optional `EmbeddedNats.start()` → `nats.connect()` → lockfile → hub wiring (reuse `bootstrap_stores`/`bootstrap_wiring`) → adapter wiring → signal handler → event loop |
| 3.2 | Wire hub internals | same file | `open_stores()`, load configs, build Hub, register agents, create `NatsChannelProxy` per bot, register dispatchers. Wire `alias_store` into PrefsStore, Hub, and agent MemoryManagers (port from multibot.py:190-229) |
| 3.3 | Wire adapters | same file | For each platform/bot: create adapter, fetch credentials from `CredentialStore`, create `NatsBus`, run as asyncio task. Close credential store before polling phase |
| 3.4 | Implement shutdown sequence | same file | Signal → cancel tasks → `teardown_buses` + `teardown_dispatchers` → close PairingManager → drain CliPool → `EmbeddedNats.stop()` → close NATS → release lockfile |
| 3.5 | Update `__main__.py` | `src/lyra/__main__.py` | Point `_run_server()` at `_bootstrap_unified`. Remove `_bootstrap_multibot` import |
| 3.6 | Update `cli.py` | `src/lyra/cli.py` | Add `lyra start` command calling unified bootstrap |

**RED-GATE after 3.6:** `pytest` full suite — all tests pass. Manual smoke: `lyra start` (no NATS_URL) boots and connects.

## Slice 4 — Remove multibot + docs

**Agents:** `backend-dev` (deletions), `tester` (test verification), `doc-writer` (docs)

**Depends on:** Slice 3

| # | Task | Agent | File(s) | Action |
|---|------|-------|---------|--------|
| 4.1 | Delete multibot files | `backend-dev` | `src/lyra/bootstrap/multibot.py`, `src/lyra/bootstrap/multibot_lifecycle.py` | `git rm` both files |
| 4.2 | Verify no dangling imports | `tester` | all `src/` and `tests/` | `grep -r "multibot"` returns zero hits (except git history) |
| 4.3 | Full test suite | `tester` | — | `pytest` green with zero test deletions |
| 4.4 | Update `docs/GETTING-STARTED.md` | `doc-writer` | `docs/GETTING-STARTED.md` | `lyra start` as default command, NATS setup optional for multi-machine |
| 4.5 | Update `docs/README.md` + `docs/ARCHITECTURE.md` | `doc-writer` | `README.md`, `docs/ARCHITECTURE.md` | Remove "legacy single-process" references, document unified bootstrap |
| 4.6 | Update `docs/COMMANDS.md` + `CLAUDE.md` + `deploy/provision.sh` | `doc-writer` | `docs/COMMANDS.md`, `CLAUDE.md`, `deploy/provision.sh` | Remove "replaces python -m lyra", add `lyra start` entry, NATS "Recommended" not "Optional" |

**RED-GATE after 4.3:** Full `pytest` pass. After 4.6: `grep -r "legacy single-process\|_bootstrap_multibot" docs/ CLAUDE.md` returns zero hits.

## Agent Summary

| Agent | Tasks | Slices |
|-------|-------|--------|
| `backend-dev` | 1.1–1.3, 2.1–2.4, 3.1–3.6, 4.1 | 1, 2, 3, 4 |
| `tester` | 1.4, 4.2, 4.3 | 1, 4 |
| `doc-writer` | 4.4, 4.5, 4.6 | 4 |

## Parallel Execution Map

```
Phase 1 (parallel):
  ├── backend-dev: Slice 1 (1.1–1.3)
  ├── tester: 1.4 (after 1.3)
  └── backend-dev: Slice 2 (2.1–2.4)

Phase 2 (sequential, after Phase 1):
  └── backend-dev: Slice 3 (3.1–3.6)

Phase 3 (parallel, after Phase 2):
  ├── backend-dev: 4.1
  ├── tester: 4.2, 4.3
  └── doc-writer: 4.4, 4.5, 4.6
```

## Success Criteria

Per spec — all 13 criteria. Key gates:

1. `lyra start` without `NATS_URL` → embedded nats-server auto-starts, hub + adapters run
2. `lyra start` with `NATS_URL` → connects to external NATS, no embedded server
3. `lyra hub` / `lyra adapter` unchanged
4. Ctrl+C → clean shutdown within 3s, no orphan processes
5. Full test suite green, zero test deletions
6. No `multibot` imports remain in codebase
7. Docs updated: no "legacy single-process" references
