### Summary
No security issues found in partition P4 (Processors, auth). The code exhibits strong secure-by-default patterns including SSRF guards, tool-output redaction, and NATS-safe error sanitization.

### Findings
| severity | file | line | description |
|---|------|------|-------------|

(No issues found)

### Metrics
- files_audited: 16
- issues_found: 0
- P0: 0 | P1: 0 | P2: 0 | P3: 0

### Recommendations (prioritized, with effort estimate)

1. **Keep current SSRF and injection defenses as reference patterns** (effort: 0)
   - `src/lyra/core/processors/_scraping.py:70-91` enforces HTTPS-only and rejects private/LAN IPs via `_is_private_ip()`.
   - `src/lyra/core/processors/_scraping.py:130-145` truncates and HTML-escapes scraped content before LLM injection.
   - `src/lyra/core/processors/search.py:46-51` HTML-escapes vault search results before injection.

2. **Maintain tool-output redaction boundary** (effort: 0)
   - `src/lyra/core/processors/stream_tool.py:44-73` uses a secure-by-default allowlist (`_NON_SENSITIVE_TOOL_NAMES`) and redacts everything else. Keep this pattern when adding new tools.

3. **Preserve NATS-safe error handling** (effort: 0)
   - `src/lyra/core/processors/stream_processor.py:192-200` intentionally uses `type(exc).__name__` instead of `str(exc)` to avoid leaking file paths, hostnames, or auth tokens on the NATS bus.
   - `src/lyra/core/processors/stream_processor.py:226-238` scrubs raw upstream wire content via `SanitizedError.from_message()` before NATS broadcast.

4. **Consider adding a comment/annotation for `_scraping.py:39` socket usage** (effort: XS)
   - `socket.getaddrinfo` is used in `asyncio.to_thread` for SSRF validation. This is correct, but a brief inline comment noting it is intentionally blocking-safe would help future reviewers.

### Clean patterns verified
- No `eval`, `exec`, `pickle`, `marshal`, `yaml.load`, or `subprocess` calls in code.
- No `password`, `token`, or `secret` variables in code (only security-conscious comments).
- No `importlib`, `__import__`, or dynamic code execution.
- `getattr` usage at `stream_processor.py:215` is safe (standard `aclose` check on async iterators).
