### Summary
One resource-leak/DoS finding (unclosed `mkstemp` fd) in Telegram audio download; otherwise clean — no credential leaks, no eval/exec, no injection vectors, no unsafe deserialization, and no authZ gaps in the audited partition.

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P2 | `src/lyra/adapters/telegram/telegram_audio.py` | 63 | `_download_audio` discards the file descriptor returned by `tempfile.mkstemp(suffix=".ogg", dir=adapter._audio_tmp_dir)`, leaking an open fd per voice/audio download. Under sustained load this exhausts the per-process file-descriptor limit (DoS). The file itself is unlinked by the caller in `telegram_inbound.py`, but the fd remains open. |

### Metrics
- files_audited: 58
- issues_found: 1
- P0: 0 | P1: 0 | P2: 1 | P3: 0

### Recommendations
1. **Close the `mkstemp` fd** in `src/lyra/adapters/telegram/telegram_audio.py` (line 63) by capturing the returned fd and calling `os.close(fd)` immediately after the path is obtained, or switch to `tempfile.NamedTemporaryFile(delete=False)` which avoids the fd-leak pattern entirely.
2. **Verify fd cleanup** in `telegram_inbound.py` (caller) to ensure the temp file is removed even if `read_bytes()` raises an exception — the current `finally` block with `tmp_path.unlink(missing_ok=True)` is correct, but the fd leak is upstream.
3. **No other action required** for the remaining patterns searched (`eval`, `exec`, `subprocess`, `pickle`, `yaml.load`, `marshal`, `compile`, `shell=True`, `os.system`, `__import__`) — all were absent or safely used in the audited partition.
