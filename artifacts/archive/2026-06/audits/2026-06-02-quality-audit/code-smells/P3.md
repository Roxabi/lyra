### Summary
P3 (Messaging, Pool, Stores, Memory) shows 12 code-smell issues across 30 files. The highest risk is `process_one` (165 lines, P0), which carries the core turn-processing path with streaming/non-streaming bifurcation, processor hooks, and session ID updates. The `Pool` class shows god-class symptoms (25+ attributes, 95-line `__init__`). DRY violations are concentrated in the pool execution path and memory upserts.

### Findings
| severity | file | line | description |
|----------|------|------|-------------|
| P0 | src/lyra/core/pool/pool_processor_exec.py | 106 | `process_one` is 165 lines, marked `noqa: C901, PLR0915`; combines streaming vs non-streaming bifurcation, processor pre/post hooks, session ID updates, error handling, and turn logging |
| P1 | src/lyra/core/pool/pool.py | 32 | `Pool.__init__` is 95 lines with 14 parameters, marked `noqa: PLR0913`; complex nested conditional logic for config override fallbacks |
| P1 | src/lyra/core/pool/pool.py | 72 | `Pool` sets 25+ attributes in `__init__`, mixing session state, callbacks, config, queues, and flags — god-class symptom |
| P1 | src/lyra/core/pool/pool_processor.py | 97 | `PoolProcessor._process_with_cancel` is 90 lines; complex cancel-in-flight race logic with nested `try/except` blocks and `asyncio.wait` |
| P2 | src/lyra/core/pool/pool_processor_exec.py | 233 | `isinstance(result, Response)` checked twice (lines 233 and 240) with identical `pyright: ignore[reportUnnecessaryIsInstance]` comments; second block could merge into first |
| P2 | src/lyra/core/pool/pool_processor_exec.py | 222 | Session ID update pattern (`end_session_async` + `session_id =`) duplicated at lines 222-224 and 236-238; also duplicated in `pool_processor_streaming.py` lines 84-86 |
| P2 | src/lyra/core/pool/pool_processor_exec.py | 252 | `str(_reply_id) if _reply_id is not None else None` repeated at lines 252, 258 and in `pool_processor_streaming.py` lines 95, 100 |
| P2 | src/lyra/core/pool/pool_observer.py | 98 | Five methods (`end_session_async`, `log_turn_async`, `session_update_async`, `append`, `index_turn_async`) share identical `try/except Exception: log.error(..., exc_info=True)` boilerplate |
| P2 | src/lyra/core/memory/memory_upserts.py | 113 | `upsert_concept` (lines 113-180) and `upsert_preference` (lines 182-238) share ~70% structural similarity: SELECT query, `is_stale` check, UPDATE/INSERT branch, `await db.commit()` |
| P2 | src/lyra/core/pool/pool_processor_exec.py | 84 | Four broad `except Exception:` / `except BaseException:` catches in the main processing path (lines 84, 153, 179, 217) — documented as debt but still swallow unexpected errors |
| P3 | src/lyra/core/messaging/render_events.py | 113 | `TextChunkRenderEvent` is defined but never emitted by `StreamProcessor`; docstring explicitly states "DEFINED BUT NOT EMITTED" |
| P3 | src/lyra/core/stores/json_agent_store.py | 75 | `(platform, bot_id)` tuple-to-string key serialization (`f"{p}:{b}"`) duplicated in `connect` (lines 75-80) and `_persist` (lines 223-224) |

### Metrics
- files_audited: 30
- issues_found: 12
- P0: 1 | P1: 3 | P2: 5 | P3: 3

### Recommendations (prioritized, with effort estimate)
1. **Extract session ID update helper** (P2, pool_processor_exec.py + pool_processor_streaming.py) — `low` effort. Unify the `end_session_async` + `session_id =` pattern into a single method on `PoolObserver` or `Pool`.
2. **Merge duplicate `isinstance(result, Response)` blocks** (P2, pool_processor_exec.py) — `low` effort. The turn-logging block (lines 240-263) can be merged into the first `isinstance(result, Response)` block (lines 233-238).
3. **Extract `reply_id_str` helper** (P2, pool_processor_exec.py + pool_processor_streaming.py) — `low` effort. Small inline function to replace the 4 repeated ternary conditionals.
4. **Extract `safe_publish` wrapper for PoolObserver** (P2, pool_observer.py) — `low` effort. A decorator or private helper to factor out the repeated `try/except Exception: log.error(..., exc_info=True)` pattern.
5. **Extract shared upsert pattern in memory_upserts.py** (P2, memory_upserts.py) — `medium` effort. `upsert_concept` and `upsert_preference` can share a private `_upsert_entry` helper that takes the SQL fragments and merge logic as parameters.
6. **Simplify `Pool.__init__` config override logic** (P1, pool.py) — `low` effort. Extract a private `_resolve_override(param, cfg_attr)` helper to remove the 6 repetitive ternary lines.
7. **Refactor `process_one` into smaller functions** (P0, pool_processor_exec.py) — `high` effort. Defer to the stage-axis refactor (#1277). The streaming path and non-streaming path should become separate top-level functions; processor hooks should be extracted.
8. **Remove or implement `TextChunkRenderEvent`** (P3, render_events.py) — `low` effort. Either wire it up in `StreamProcessor` or delete it to avoid dead code.
