# Copy to ~/.roxabi/factory/env/cloudflared.env (chmod 600)
# Connector token from Cloudflare Tunnel "factory-ingress" (remotely-managed).
# Public hostname: ingress.roxabi.dev → http://factory-ingress:8780

TUNNEL_TOKEN=