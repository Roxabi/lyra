### Summary
Partition P2 (Agent model, CLI, commands) contains 38 files. Several long functions are already acknowledged with `noqa` markers, but significant DRY violations remain: `CliPool.send()` and `send_streaming()` duplicate ~40 lines of process spawn/respawn logic with a latent inconsistency on model_config mismatch. `BotRow.from_db_row()` repeats identical JSON parsing blocks three times. `CommandRouter` and `AgentBase` carry moderate complexity.

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P1 | src/lyra/core/cli/cli_pool_send.py | 55 | `send()` and `send_streaming()` (cli_pool_streaming.py:38) duplicate ~40 lines of process spawn/respawn logic (entry liveness, system_prompt change, stale resume, error handling). They diverge on model_config mismatch: `send` warns and ignores (line 107), while `send_streaming` kills and respawns (line 85). This is both a DRY violation and a latent inconsistency. |
| P2 | src/lyra/core/agent/agent_db_loader.py | 35 | `agent_row_to_config()` is 149 lines with many independent branches. Acknowledged (`noqa: C901, PLR0915`) but still a readability burden. |
| P2 | src/lyra/core/agent/agent_seeder.py | 55 | `_parse_toml()` is 105 lines. Acknowledged (`noqa: PLR0915`) but remains a long function. |
| P2 | src/lyra/core/cli/protocol/cli_non_streaming.py | 61 | `read_until_result()` is 148 lines with nested timeout/EOF/error handling. Acknowledged (`noqa: C901, PLR0915`). |
| P2 | src/lyra/core/cli/cli_pool_send.py | 55 | `send()` is 110 lines. Acknowledged (`noqa: C901,PLR0913`). |
| P2 | src/lyra/core/cli/cli_pool_streaming.py | 38 | `send_streaming()` is 106 lines. Acknowledged (`noqa: C901,PLR0913`). |
| P2 | src/lyra/core/cli/protocol/cli_streaming.py | 72 | `StreamingIterator.__anext__()` is 86 lines with nested while/try/if. Acknowledged (`noqa: C901`). |
| P2 | src/lyra/core/agent/bot_models.py | 55 | `BotRow.from_db_row()` repeats identical JSON-parse-and-validate pattern 3x for `owner_users`, `trusted_users`, `trusted_roles` (lines 74-107). |
| P2 | src/lyra/core/commands/command_router.py | 257 | `dispatch()` has 66 lines and high cyclomatic complexity (C901). |
| P2 | src/lyra/core/agent/agent.py | 46 | `AgentBase.__init__()` has 11 parameters. Acknowledged (`noqa: PLR0913`). |
| P3 | src/lyra/core/agent/agent_loader.py | 1 | Entire file is a re-export wrapper after TOML path removal (#346). No unique logic; adds import indirection. |
| P3 | src/lyra/core/cli/cli_pool_spawn.py | 111 | `_identity_env()` uses `strip = lambda` (noqa: E731). Should be a named function. |
| P3 | src/lyra/core/commands/builtin_commands.py | 28 | `HelpCommandDeps` is a 13-field parameter bag serving two unrelated commands (`help_command` and `config_command`). |
| P3 | src/lyra/core/commands/command_router.py | 174 | `_build_builtin_handlers()` defines 10 inline lambdas. Consider small named methods for traceability. |

### Metrics
- files_audited: 38
- issues_found: 14
- P0: 0 | P1: 1 | P2: 9 | P3: 4

### Recommendations (prioritized, with effort estimate)
1. **Extract shared spawn/respawn logic** (P1, ~3h). Create a private `_ensure_entry()` method that both `send` and `send_streaming` call. This eliminates the ~40-line duplication and resolves the model_config mismatch divergence.
2. **Extract JSON parse helper in BotRow** (P2, ~30min). Replace the 3 duplicated try/except blocks with a single `_parse_json_list_of_strings()` helper.
3. **Decompose agent_row_to_config** (P2, ~2h). Split field-by-field parsing into small helper functions per concern (voice, smart_routing, workspaces, patterns).
4. **Split CommandRouter.dispatch** (P2, ~1h). Extract `_dispatch_plugin`, `_dispatch_session`, `_dispatch_builtin` into clearer sub-methods.
5. **Remove agent_loader.py re-export** (P3, ~15min). Fold import into consumers or delete the wrapper.
6. **Replace lambda with def** (P3, ~5min). Convert `_identity_env` lambda to a named function.
