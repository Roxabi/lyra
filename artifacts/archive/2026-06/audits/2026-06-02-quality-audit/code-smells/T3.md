### Summary
T3 partition shows moderate code-smell density concentrated in three files: `test_blobstore_adapter.py` (15 inline imports of the same module), `test_render_event_codec.py` (39 repetitive codec instantiations), and `test_nats_channel_proxy.py` (27 identical mock setups). The dominant pattern is DRY violation via copy-pasted test boilerplate; no critical P0 issues found.

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P2 | tests/infrastructure/test_blobstore_adapter.py | 100, 121, 137, 165, 208, 240, 256, 271, 295, 330, 370, 399, 430, 458, 493 | 15 inline imports of `lyra.infrastructure.blobstore_adapter.HttpBlobStoreAdapter` — DRY violation, should be module-level import |
| P2 | tests/infrastructure/test_blobstore_adapter.py | 329, 369, 398, 429, 457 | 5 inline imports of `roxabi_contracts` — repeated inside every test method |
| P3 | tests/infrastructure/test_blobstore_adapter.py | 367, 396, 427, 455 | 4 inline imports of `httpx` — repeated inside error-translation tests |
| P2 | tests/nats/test_render_event_codec.py | 49, 139, 265, 325, 443, 502, 693 | 39 instantiations of `NatsRenderEventCodec()` across 7 classes — repetitive boilerplate; could use a shared fixture or parameterized test helper |
| P2 | tests/nats/test_render_event_codec.py | 142, 157, 176, 187, 198, 211, 268, 282, 297, 370, 377, 384, 391, 700, 712, 721 | 16 `assert decoded == original` assertions — identical assertion repeated per event-type test; candidate for parameterized round-trip helper |
| P2 | tests/nats/test_nats_channel_proxy.py | 78, 92, 113, 128, 150, 167, 194, 215, 245, 265, 282, 299, 362, 397, 426, 462, 503, 522, 543, 579, 612, 650, 673, 705, 721, 734, 778, 826 | 27 identical `nc = _make_nc()` setups; 24 `proxy = NatsChannelProxy(nc=nc, platform=Platform.TELEGRAM, bot_id="main")` and 22 `inbound = _make_inbound(...)` — extreme copy-paste repetition; pytest fixture or shared setup would cut 60+ lines |
| P2 | tests/nats/test_nats_bus.py | 70, 79, 93, 102, 115, 135, 164, 216, 225, 234, 261, 279, 312, 372, 389, 405, 420, 462, 479, 582, 602, 622, 656, 694 | 13 `bus = _make_bus(nc)` + 12 `await bus.stop()` pairs — repetitive setup/teardown; `pytest.fixture` with cleanup would eliminate duplication |
| P2 | tests/nats/test_keepalive.py | 35 | `_make_nc()` and `_make_inbound()` helpers duplicate `tests/nats/test_nats_channel_proxy.py` lines 40 and 50 almost identically — cross-file DRY violation; extract to shared `tests/nats/helpers.py` |
| P2 | tests/nats/test_nats_channel_proxy.py | 40 | `_make_nc()` and `_make_inbound()` duplicated in `test_keepalive.py` — cross-file DRY violation (same finding, mirror location) |
| P2 | tests/nats/test_hub_standalone.py | 294 | `test_trust_re_resolution_invoked` spans 94 lines — long function; contains nested try/finally + asyncio.sleep polling loop; should split into Arrange helper and Act/Assert sections |
| P2 | tests/nats/test_nats_bus.py | 312 | `test_queue_group_distributes_messages` is ~50 lines with 3-level nesting (try→while→while) and 6 branches — high cyclomatic complexity; could extract drain-helpers |
| P2 | tests/nats/test_nats_bus.py | 582 | `TestNatsBusVersionMismatch` class (lines 582–714) contains 6 test methods each with identical `try/finally: await bus.stop()` pattern — repetitive teardown across 130 lines |
| P2 | tests/transport/test_turn_publisher.py | 58, 77, 95, 128, 146, 165, 182, 200, 217, 239, 256 | 11 test methods repeat `_make_publisher()` + `await publisher.publish_*` + `_decode_published_event` — candidate for parameterized test or shared fixture |
| P3 | tests/nats/test_sanitize.py | 61, 88, 119, 155, 188, 222, 255, 286 | 8 test methods inline-import `TrustLevel`, `InboundMessage`, `Platform`, `TelegramMeta`/`DiscordMeta`/`GenericMeta` and `serialize`/`deserialize`/`TYPE_REGISTRY_RESOLVER` — repeated inline imports across 200 lines |
| P3 | tests/nats/test_nats_channel_proxy.py | 462 | `test_render_audio_puback_fail_dispatches_notification` is 37 lines with 7 assertions — does multiple things (notif subject, payload, content, error masking); could split into 2 focused tests |
| P3 | tests/infrastructure/test_blobstore_adapter.py | 447 | `test_put_5xx_error_message_does_not_contain_url` is 24 lines with inline `httpx.Request` and `httpx.Response` setup — setup logic duplicated across error-translation tests |
| P3 | tests/nats/test_sanitize.py | 152 | `_FakeTurnStore` class (lines 31–50) duplicates `tests/core/conftest.py` TurnStore mocking pattern — check if shared stub exists; if not, extract to `tests/helpers/turn_store.py` |
| P3 | tests/nats/test_keepalive.py | 104 | `_slow_events()` async generator inside `test_publishes_keepalive_during_idle` is a 6-line nested function — could be module-level helper `_make_slow_events()` |
| P3 | tests/nats/test_nats_bus.py | 545 | `_make_inbound_bytes_*` helper quartet (lines 548–571) — 4 near-identical helpers that differ only by version mutation; could be single `_make_inbound_bytes(version=...)` with conditional logic |
| P3 | tests/nats/test_nats_channel_proxy.py | 720 | `test_publish_stream_errors_noop_when_no_active_streams` is 6 lines but still creates full `nc`/`proxy` setup — disproportionate setup-to-assert ratio; 4 lines of boilerplate for 2 assertions |
| P3 | tests/nats/test_worker_registry.py | 49 | `test_cap_drops_new_ids_at_max` seeds 20 workers in a loop — data-driven setup without parameterization; could use `@pytest.mark.parametrize` with 2 cases (at-cap, over-cap) |
| P3 | tests/transport/test_worker_pool_client.py | 50, 71, 89, 108, 122, 139, 165, 185 | 8 test methods repeat `pool, mock_transport = _make_pool_with_workers(...)` + `mock_transport.call = AsyncMock(...)` — setup pattern could be a fixture with parametrized side-effect |
| P3 | tests/nats/test_hub_standalone.py | 396 | `TestHubStandaloneIdentityName` and `TestAdapterStandaloneIdentityName` (lines 396–508) both mock `nats_connect` with `SystemExit` sentinel pattern — 112 lines of near-identical mocking logic; could share a `_mock_connect_sentinel` helper |
| P3 | tests/nats/test_render_event_wire_roundtrip.py | 131 | `_make_inbound()` helper duplicates `test_nats_channel_proxy.py` `_make_inbound()` and `test_keepalive.py` `_make_inbound()` — third cross-file duplicate of the same helper |
| P3 | tests/infrastructure/test_blobstore_adapter.py | 29 | `_make_storage_ref()` helper is 40 lines with branching on `is_sentinel` — high complexity for a factory; could split into `_make_storage_ref()` and `_make_sentinel_ref()` |
| P3 | tests/infrastructure/turn_writer/test_writer.py | 112 | `_count_turns()`, `_get_session()`, `_count_processed_events()` are 3 nearly-identical SQL helpers that differ only by query/params — could be generic `_sql_scalar(query, params)` and `_sql_row(query, params)` |

### Metrics
- files_audited: 35
- issues_found: 25
- P0: 0 | P1: 0 | P2: 14 | P3: 11

### Recommendations (prioritized, with effort estimate)
1. **Extract shared NATS mock helpers** (`_make_nc`, `_make_inbound`) to `tests/nats/helpers.py` — eliminates 3 cross-file duplicates, ~40 lines saved. Effort: S (1 file, 2 imports updated). Target: `test_nats_channel_proxy.py`, `test_keepalive.py`, `test_render_event_wire_roundtrip.py`.
2. **Move `test_blobstore_adapter.py` inline imports to module top** — 15 `HttpBlobStoreAdapter`, 5 `roxabi_contracts`, 4 `httpx` imports. Effort: S (string replacement, no logic change). Target: `tests/infrastructure/test_blobstore_adapter.py`.
3. **Introduce `codec` fixture in `test_render_event_codec.py`** — shared `NatsRenderEventCodec()` instance + parameterized round-trip helper. Effort: S (fixture + parametrize decorator). Target: `tests/nats/test_render_event_codec.py`.
4. **Add `bus` pytest fixture with auto-cleanup to `test_nats_bus.py`** — wraps `_make_bus(nc)` + `register()` + `start()` + `yield` + `stop()` in `try/finally`. Effort: S (1 fixture, 12 call sites updated). Target: `tests/nats/test_nats_bus.py`.
5. **Split `test_trust_re_resolution_invoked` into helper + 2 focused tests** — extract Hub+NatsBus setup into `_make_hub_bus(nc, nats_server_url)` helper, separate `test_message_consumed` and `test_authenticator_called`. Effort: S (1 helper, 1 split). Target: `tests/nats/test_hub_standalone.py`.
6. **Parameterize `test_turn_publisher.py` publish methods** — single `_test_publish_kind` parameterized over `(method_name, expected_kind, extra_assertions)`. Effort: M (refactor 11 test methods into 1 parameterized test + 2 edge-case tests). Target: `tests/transport/test_turn_publisher.py`.
7. **Consolidate `_make_inbound_bytes_*` quartet in `test_nats_bus.py`** — replace 4 helpers with `_make_inbound_bytes(version=1|None|2|"1")`. Effort: S (1 helper, 6 call sites). Target: `tests/nats/test_nats_bus.py`.
8. **Extract `_FakeTurnStore` to shared `tests/helpers/`** — if not already present; prevents future cross-file duplication. Effort: S (1 file, 2 imports). Target: `tests/nats/test_sanitize.py`, `tests/core/conftest.py`.
9. **Extract `test_worker_pool_client.py` transport mock setup to fixture** — `_make_pool_with_workers` + `AsyncMock(side_effect=...)` pattern. Effort: S (1 fixture, 8 call sites). Target: `tests/transport/test_worker_pool_client.py`.
10. **Consolidate `test_blobstore_adapter.py` error-translation test setup** — shared `_make_httpx_error(status_code, ...)` helper. Effort: S (1 helper, 4 call sites). Target: `tests/infrastructure/test_blobstore_adapter.py`.
