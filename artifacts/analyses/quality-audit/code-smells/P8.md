### Summary
P8 (LLM, agents, inbound/outbound/staging, blobstore, monitoring, integrations, typing, tools) contains moderate code-smell density driven by the active stage-axis refactor (#1277). The heaviest smells are long functions in monitoring/inbound, duplicated exception-handling boilerplate in blobstore handlers, and transitional fallback code in SimpleAgent. No critical (P0) smells.

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P1 | src/lyra/agents/simple_agent.py | 205 | `process` is 92 lines with `# noqa: C901` complexity residual; mixes voice routing, LLM text building, backend linking, streaming vs non-streaming dispatch, and error classification in one method |
| P1 | src/lyra/inbound/attachment_ingest.py | 189 | `_ingest_non_audio` is 103 lines with 5 nested try/except/if/continue blocks; deep nesting makes the per-item loop hard to follow and test |
| P1 | src/lyra/blobstore/_handlers.py | 91 | 4 HTTP handlers (`handle_put`, `handle_get`, `handle_head`, `handle_delete`) share near-identical exception-mapping boilerplate (BlobWriteError → 500, BlobNotFoundError → 404, aiosqlite.Error → 500, RuntimeError → 500, generic Exception → 500) — DRY violation across 190 lines of handlers |
| P2 | src/lyra/monitoring/checks.py | 207 | `run_checks` is 118 lines; sequential check orchestration with no abstraction for the repeated `asyncio.to_thread(...)` / `checks.append(...)` pattern (14 checks) |
| P2 | src/lyra/monitoring/checks_audio.py | 82 | `check_audio_consumer_lag` is 108 lines; shares ~30 lines of identical HTTP fetch / stream validation / 404-skip logic with `check_audio_stream_usage` (line 192) — duplicated in same file |
| P2 | src/lyra/inbound/session_builder.py | 131 | `_build_thread_path` is 94 lines; inline cache LRU logic (lines 155-168) and update closure (lines 181-216) are long, deeply nested, and duplicate cache eviction logic from `_build_turnstore_path` |
| P2 | src/lyra/monitoring/checks_log.py | 17 | `check_nats_log_errors` and `check_hub_dict_stream_gen_timeout` (line 57) share identical `subprocess.run` podman-logs boilerplate (~45 lines) with only the grep pattern and result name changing — DRY violation |
| P2 | src/lyra/agents/simple_agent.py | 65 | `__init__` has 11 parameters (`# noqa: PLR0913`) — god-class wiring indicator; carries both `cli_pool` and `cli_nats_driver` as optional dual-backend paths |
| P2 | src/lyra/agents/simple_agent.py | 127 | `_register_session_commands` contains transitional fallback (lines 127-143) that constructs `SessionTools` locally when not injected — `except Exception` broad-catch with `# noqa: BLE001` hides misconfiguration |
| P2 | src/lyra/outbound/_emitter_run.py | 52 | `_run_event_loop` has `# noqa: C901`; 5 chained `isinstance` branches + nested `if isinstance` inside each branch for Run/ToolCall/Text/Reasoning events — high cyclomatic complexity |
| P2 | src/lyra/blobstore/_handlers.py | 216 | `handle_delete` has deep nesting (if → try → except → try → except → if → try → except) across 67 lines; TOCTOU comment at line 260 confirms structural complexity |
| P2 | src/lyra/inbound/attachment_ingest.py | 173 | 3 broad `except Exception` catches (lines 173, 222, 256) without `# noqa` annotations; log-and-degrade pattern is correct but unannotated, risking accidental lint drift |
| P2 | src/lyra/typing/listener.py | 92 | 2 nested broad `except Exception` catches (lines 92, 97) without `# noqa` annotations; defensive cancel pattern could be extracted to a helper |
| P2 | src/lyra/blobstore/_handlers.py | 76 | `_conn()` helper is duplicated verbatim in `src/lyra/blobstore/_keys.py` (line 18) — same function, same `# noqa: SLF001` |
| P3 | src/lyra/monitoring/checks_varz.py | 32 | `check_disk_pct` and `check_inode_pct` (line 72) share identical threshold/level/detail construction logic — only the metric source differs (shutil vs os.statvfs) |
| P3 | src/lyra/agents/simple_agent.py | 158 | `_maybe_register_reset` and `_maybe_register_resume` (line 175) share nearly identical dual-branch `cli_pool` vs `cli_nats_driver` lambda wiring — could be collapsed to a single helper |
| P3 | src/lyra/llm/decorators.py | 36 | `RetryDecorator.complete`, `RetryDecorator.stream`, `CircuitBreakerDecorator.complete`, `CircuitBreakerDecorator.stream` all carry `# noqa: PLR0913` — 4 identical Protocol-compliance signatures in one file |
| P3 | src/lyra/outbound/formatter.py | 41 | `OutboundFormatter` Protocol spans two responsibility axes (pure formatting + platform I/O); S7a accepted the SRP cost at N=2 platforms but documented re-evaluate-at-N>=3 — not a smell today, but a boundary to watch |
| P3 | src/lyra/llm/llm_client.py | 47 | `LlmClient` has 11 methods (control-plane + provider); `reset`, `resume_and_reset`, `switch_cwd` all duplicate the same `CliControlCmd` construction + `_pool._transport.call` pattern (3x) with only `op` field changing |
| P3 | src/lyra/outbound/_tool_recap.py | 114 | `_route` has `# noqa: C901` — 9 `elif` branches for tool dispatch; acceptable per inline comment but close to complexity threshold |
| P3 | src/lyra/monitoring/escalation.py | 68 | `_escalate_via_cli` constructs subprocess with hardcoded `claude` CLI path; no fallback if `claude` is not on PATH (shutil.which checked at caller, but still a single-backend dependency) |
| P3 | src/lyra/llm/base.py | 1 | Backward-compatibility shim re-exporting `LlmProvider` and `LlmResult` from `lyra.core.ports.llm` — dead code risk if not migrated |
| P3 | src/lyra/llm/llm_codec.py | 1 | Backward-compatibility shim re-exporting `CliNatsCodec` as `LlmCodec` — dead code risk if not migrated |
| P3 | src/lyra/integrations/vault_cli.py | 29 | `VaultProvider.add` Protocol and `VaultCli.add` implementation both carry `# noqa: PLR0913` — 7 params for a vault entry; the Protocol in `base.py` also carries the same annotation |
| P3 | src/lyra/tools/gh_token/dispenser.py | 58 | `Dispenser.__init__` carries `# noqa: PLR0913` — 7 params; same file also contains `RateLimiter` (clean) and `_resolve_token` (clean) |
| P3 | src/lyra/inbound/pipeline.py | 48 | `InboundPipeline.run` carries `# noqa: PLR0913` — 7 params; pipeline signature is justified but increases arity surface |
| P3 | src/lyra/tools/gh_token/helper.py | 11 | `TODO(T4)` — MintError → NATS MintFailureEvent is unimplemented (single TODO in P8) |

### Metrics
- files_audited: 55
- issues_found: 25
- P0: 0 | P1: 3 | P2: 11 | P3: 11

### Recommendations (prioritized, with effort estimate)

| # | issue | effort | action |
|---|-------|--------|--------|
| 1 | `SimpleAgent.process` complexity | M (2-3h) | Extract `voice_rewritten` handling, backend linking, and error-classification into private helper methods; `process` should only orchestrate |
| 2 | `attachment_ingest._ingest_non_audio` deep nesting | M (2-3h) | Extract per-item ingest into `_ingest_single_item` helper; flatten the loop body to `if oversize: continue; data = await fetch(); ref = await store()` |
| 3 | blobstore handler DRY | M (2-3h) | Introduce `_handle_store_op` decorator or context manager that maps `BlobWriteError`, `BlobNotFoundError`, `aiosqlite.Error`, `RuntimeError` to `(status_code, detail, audit_result)` tuples; apply to all 4 handlers |
| 4 | `checks_audio` duplication | S (1h) | Extract shared HTTP fetch / stream validation / 404-skip logic into `_fetch_jsz_data` helper |
| 5 | `checks_log` duplication | S (1h) | Extract `podman logs` subprocess wrapper into `_run_podman_logs` helper parameterized by pattern, threshold, and check name |
| 6 | `_conn()` duplication | XS (15m) | Move `_conn()` from `_handlers.py` and `_keys.py` to a shared `_conn.py` or make `_keys.py` the canonical source and import from there |
| 7 | `session_builder` cache eviction | S (1h) | Extract cache eviction into `_evict_if_full(cache)` helper used by both `_build_turnstore_path` and `_build_thread_path` |
| 8 | `SimpleAgent.__init__` arity | L (4h) | Introduce `SimpleAgentWiring` dataclass to bundle optional dependencies; `__init__` becomes `(config, provider, wiring)` |
| 9 | `LlmClient` control-plane duplication | S (1h) | Extract `_send_control_cmd(pool_id, op, **kwargs)` helper to eliminate the 3x duplicated `CliControlCmd` construction + `_pool._transport.call` pattern |
| 10 | unannotated broad catches | XS (30m) | Add `# noqa: BLE001` with explanatory comments to the 5 unannotated `except Exception` sites in `attachment_ingest.py` and `typing/listener.py` |
