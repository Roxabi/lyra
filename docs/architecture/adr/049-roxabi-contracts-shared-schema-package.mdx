---
title: "ADR-049: roxabi-contracts — Shared NATS Schemas (Voice, Image)"
description: Add a second uv workspace subpackage (packages/roxabi-contracts/) that ships subject registries, Pydantic envelope schemas, and test fixtures for every lyra-owned cross-project NATS domain. Publishers in lyra and subscribers in satellites import the same typed contracts, replacing ADR-only specifications and eliminating drift. Revised 2026-04-16 after architect + devops + security review.
---

## Status

Accepted

## Context

ADR-044 froze the lyra ↔ voiceCLI wire contract. ADR-047 codified the ownership boundary: lyra owns subjects + publishers + nkey identity; satellites own worker processes. ADR-045 extracted transport primitives into `roxabi-nats` so satellites can subscribe without importing lyra.

Two problems remain after that stack lands:

**1. Contract-as-document, not contract-as-code.**
Today the voice contract lives in ADR-044's prose + tables. The envelope fields (`contract_version`, `trace_id`, `text`, `voice`, `audio`, `sample_rate`) are re-implemented independently in lyra's `NatsTtsClient` and voiceCLI's `tts_adapter.py`. Nothing prevents a payload field rename in the ADR from silently diverging between the two repos. Grep-gate (ADR-047 §14.3) catches stray subject literals but not payload drift.

When `lyra.image.*` and `lyra.memory.*` are added as contract ADRs (per ADR-047 §4, pending work), the drift surface multiplies. Every new domain is a fresh opportunity for lyra and satellite to disagree on a field name, default, or type.

**2. E2E testing requires the full stack.**
Today, running an end-to-end voice test from lyra requires:
- NATS server running
- voiceCLI worker running, with GPU and Chatterbox/Whisper models loaded
- lyra hub wired to the real NATS URL

This is expensive in CI and impossible in laptop development. voiceCLI#69 (SDK adoption) is blocked in part because E2E tests on the lyra side cannot be run without spinning up real TTS/STT. The same pain will recur for imageCLI and any future satellite.

The subscriber cannot be moved into lyra (ADR-047 rejected that — satellites must own their workers so they can run on GPU machines). So lyra needs a way to test its publisher without pulling satellite code, and satellites need a way to test their subscriber without pulling lyra.

A shared contracts package — domain schemas + subject constants + canned fixtures, with zero runtime dependency on either lyra or any satellite — solves both problems in one artifact.

## Options Considered

### Option A: Keep contracts in ADR documents only (status quo)

- **Pros:** Zero new packages. Contracts are human-readable. Works for small domain counts.
- **Cons:** Drift is undetectable at type-check time. Every new cross-project domain doubles the re-implementation surface (lyra publisher + satellite subscriber). E2E testing pain persists — no shared fixtures exist. Fails the "best microkernel" criterion that the kernel/plugin boundary be explicit in code, not in prose.

### Option B: Fold schemas into `roxabi-nats`

- **Pros:** One package, fewer workspace members. Satellites already depend on `roxabi-nats` — no new dependency arrow.
- **Cons:** Violates ADR-047's explicit rule: `roxabi-nats` is pure transport with zero knowledge of `lyra.*` subjects. Adding `roxabi_nats.contracts.voice` would mean external consumers of the transport SDK (including future non-Roxabi users) must depend on Lyra-specific domain schemas. Breaks transport/contract separation — the two coordinates evolve on different cadences (`roxabi-nats` = transport bugs; contracts = payload evolution).

### Option C: Contract package per domain (`roxabi-voice-contract`, `roxabi-image-contract`, ...)

- **Pros:** Each contract versions independently. Satellites depend only on the contract they implement.
- **Cons:** Explosion of workspace members. Shared infrastructure (envelope base, `trace_id`, `contract_version`, test-double pattern) must be duplicated across every contract package or extracted into a `roxabi-contracts-base` package — re-creating Option D's structure under a different name.

### Option D: Single `roxabi-contracts` package, one module per domain — **chosen**

- **Pros:**
  - Single source of truth. Publisher and subscriber import identical Pydantic models; drift becomes a type error.
  - One package, multiple domains — shared envelope helpers (`ContractEnvelope` base, `trace_id` semantics, `contract_version` mixin) live once, not per-domain.
  - Test fixtures (silence WAV, pixel PNG, canned memory query results) ship with the contract — every publisher and subscriber gets them for free.
  - Enables the pytest test-double pattern: `roxabi_contracts.voice.testing.FakeTtsWorker` subscribes to `lyra.voice.tts.request` and replies with fixture data. Lyra E2E runs in-process, no GPU, no voiceCLI. Satellite E2E runs in-process, no lyra hub.
  - Clean extension: adding `lyra.memory.*` or `lyra.llm.*` is a new submodule + tag bump, not a new repo or package.
- **Cons:**
  - One contract bump in one domain forces consumers of *other* domains to pick up a new version on their next lock update (if they pin tightly). Mitigated by semver: patch/minor for additive changes, major for breaks.
  - Workspace member count grows from 1 (`roxabi-nats`) to 2. `uv.lock` size grows modestly.

## Decision

Add `packages/roxabi-contracts/` as a second uv workspace subpackage within the lyra monorepo. Ship one submodule per lyra-owned cross-project domain. Lyra publishers (Cohort B in `src/lyra/nats/`) and satellite subscribers both import from it.

### Layout (v0.1.0 — voice-only)

```
lyra/
├── pyproject.toml                          # [tool.uv.workspace] members += ["packages/roxabi-contracts"]
├── packages/
│   ├── roxabi-nats/                        # ADR-045, unchanged
│   └── roxabi-contracts/
│       ├── pyproject.toml                  # name = "roxabi-contracts", pydantic>=2; roxabi-nats in [testing] extra only
│       ├── README.md                       # links to ADR-044 (voice), future ADRs (image/memory/llm)
│       ├── src/roxabi_contracts/
│       │   ├── __init__.py                 # re-exports ContractEnvelope, CONTRACT_VERSION
│       │   ├── envelope.py                 # ContractEnvelope base: contract_version, trace_id, issued_at — PURE PYDANTIC, zero transport imports
│       │   └── voice/
│       │       ├── __init__.py             # re-exports SUBJECTS + models (no testing imports at package root)
│       │       ├── subjects.py             # SUBJECTS namespace — string constants
│       │       ├── models.py               # TtsRequest, TtsResponse, SttRequest, SttResponse — ConfigDict(extra="ignore")
│       │       ├── fixtures.py             # PURE DATA — silence_wav_16khz (synthesized), sample_transcript_en, no NATS imports
│       │       └── testing.py              # FakeTtsWorker, FakeSttWorker — requires [testing] extra, production guard-rails
│       └── tests/
│           ├── test_envelope.py
│           ├── test_voice_models.py
│           ├── test_voice_extra_ignore.py  # asserts unknown fields pass (forward-compat invariant)
│           └── test_voice_testing_doubles.py
```

Image, memory, and llm submodules are added in later versions after their own contract ADRs land. They do **not** ship as placeholder directories in v0.1.0 — creating empty modules would let `import roxabi_contracts.image` succeed silently with missing attributes.

## NATS Contracts

The following subsections define the wire contracts for each NATS domain. These definitions are normative — the Pydantic models in `roxabi_contracts.<domain>.models` implement these schemas.

### Voice Contract (from ADR-044)

#### Subjects

| Subject | Direction | Producer | Consumer | Purpose |
|---|---|---|---|---|
| `lyra.voice.stt.request` | request-reply | `NatsSttClient.transcribe` (hub) | `SttAdapterStandalone.handle` (satellite) | Transcribe audio to text |
| `lyra.voice.tts.request` | request-reply | `NatsTtsClient.synthesize` (hub) | `TtsAdapterStandalone.handle` (satellite) | Synthesize speech from text |
| `lyra.voice.stt.heartbeat` | fire-and-forget | `SttAdapterStandalone` (satellite) | `NatsSttClient._on_heartbeat` (hub) | Liveness + capacity |
| `lyra.voice.tts.heartbeat` | fire-and-forget | `TtsAdapterStandalone` (satellite) | `NatsTtsClient._on_heartbeat` (hub) | Liveness + capacity |

Queue groups (load balancing across satellite replicas): `STT_WORKERS` for STT, `TTS_WORKERS` for TTS. Replies use the NATS request-reply inbox; no dedicated reply subject.

#### Payload encoding

UTF-8 JSON. Binary audio is base64-encoded in-payload (no sidecar stream). NATS `max_payload` is the hard ceiling; requests that exceed it fail fast with `STTUnavailableError("STT request payload too large")` / `TtsUnavailableError("TTS request payload too large")` on the hub side.

#### `contract_version` — additive, one-way

Every payload listed below carries `contract_version: "1"` as a top-level string field.

- **Producers (hub or satellite) MUST emit it** on every outgoing request, reply, and heartbeat.
- **Consumers MUST ignore unknown values.** A consumer reading a payload uses `.get("contract_version")` (or simply does not read it at all); it MUST NOT reject or error on any value. `"1"`, `"2"`, `"whatever"`, or missing — all are accepted.
- The field is **additive**, not a handshake. A `"2"` sender and a `"1"` reader continue to work as long as no other field has been removed or reinterpreted between versions. When an incompatible change is needed, it ships as a new subject (or a new ADR), not a bumped version.
- The goal is **observability** — logs and metrics on both sides can filter by version, and operators can diagnose mismatches on the wire without guessing.

#### Request — `lyra.voice.stt.request`

```json
{
  "contract_version": "1",
  "request_id": "b6d5a3a0-7e70-4e3a-ae93-3a9f1c2b7c5e",
  "audio_b64": "T2dnUwACAAAA...",
  "mime_type": "audio/ogg",
  "model": "large-v3-turbo",
  "language_detection_threshold": 0.5,
  "language_detection_segments": 3,
  "language_fallback": "en"
}
```

| Field | Type | Required | Notes |
|---|---|:---:|---|
| `contract_version` | string | ✓ | Always `"1"` for this ADR |
| `request_id` | string (uuid4) | ✓ | Echoed back in the reply |
| `audio_b64` | string (base64) | ✓ | Raw audio bytes |
| `mime_type` | string | ✓ | `audio/ogg`, `audio/mpeg`, `audio/wav`, `audio/mp4`, `audio/webm`, `audio/flac`; unknown → treated as `audio/ogg` |
| `model` | string | ✓ | Whisper model name (e.g. `large-v3-turbo`) |
| `language_detection_threshold` | number \| null | — | Overrides STT config if non-null |
| `language_detection_segments` | integer \| null | — | Overrides STT config if non-null |
| `language_fallback` | string \| null | — | ISO-639-1 code; fallback when detection confidence is below threshold |

#### Reply — `lyra.voice.stt.request` (success)

```json
{
  "contract_version": "1",
  "request_id": "b6d5a3a0-7e70-4e3a-ae93-3a9f1c2b7c5e",
  "ok": true,
  "text": "bonjour tout le monde",
  "language": "fr",
  "duration_seconds": 1.42
}
```

#### Reply — `lyra.voice.stt.request` (error)

```json
{
  "contract_version": "1",
  "request_id": "b6d5a3a0-7e70-4e3a-ae93-3a9f1c2b7c5e",
  "ok": false,
  "error": "transcription_failed"
}
```

Error codes are short snake_case tokens. The hub raises `STTUnavailableError` on `ok: false` regardless of the specific `error` value — the token is for logs, not for control flow.

#### Request — `lyra.voice.tts.request`

```json
{
  "contract_version": "1",
  "request_id": "2f77f4fa-58c2-4d35-8c9e-2e7c3f9c8f1e",
  "text": "hello world",
  "language": "en",
  "voice": "alice",
  "fallback_language": "en",
  "chunked": true,
  "engine": "chatterbox",
  "speed": 1.0
}
```

| Field | Type | Required | Notes |
|---|---|:---:|---|
| `contract_version` | string | ✓ | Always `"1"` |
| `request_id` | string (uuid4) | ✓ | Echoed back |
| `text` | string | ✓ | UTF-8 text to synthesize |
| `language` | string \| null | — | ISO-639-1 code; caller override |
| `voice` | string \| null | — | Engine-specific voice id; caller override |
| `fallback_language` | string \| null | — | Used when `language` is ambiguous |
| `chunked` | boolean | — | Enables multi-segment synthesis; default `true` |
| _agent TTS fields_ | various \| null | — | `engine`, `accent`, `personality`, `speed`, `emotion`, `exaggeration`, `cfg_weight`, `segment_gap`, `crossfade`, `chunk_size`, `default_language`, `languages` — forwarded verbatim when set; omitted when null. See `_TTS_CONFIG_FIELDS` / `_AGENT_TTS_FIELDS` for the authoritative list. |

#### Reply — `lyra.voice.tts.request` (success)

```json
{
  "contract_version": "1",
  "request_id": "2f77f4fa-58c2-4d35-8c9e-2e7c3f9c8f1e",
  "ok": true,
  "audio_b64": "T2dnUwACAAAA...",
  "mime_type": "audio/ogg",
  "duration_ms": 1420,
  "waveform_b64": null
}
```

`mime_type` defaults to `audio/ogg` on the hub side if absent. `duration_ms` and `waveform_b64` are both optional/nullable; renderers must handle nulls.

#### Reply — `lyra.voice.tts.request` (error)

```json
{
  "contract_version": "1",
  "request_id": "2f77f4fa-58c2-4d35-8c9e-2e7c3f9c8f1e",
  "ok": false,
  "error": "synthesis_failed"
}
```

#### Heartbeats — `lyra.voice.{stt,tts}.heartbeat`

```json
{
  "contract_version": "1",
  "worker_id": "stt-roxabituwer-1712934123",
  "service": "stt_workers",
  "host": "roxabituwer",
  "subject": "lyra.voice.stt.request",
  "queue_group": "stt_workers",
  "ts": 1712934198.123,
  "model_loaded": "large-v3-turbo",
  "vram_used_mb": 4128,
  "vram_total_mb": 10240,
  "active_requests": 0
}
```

| Field | Type | Required | Notes |
|---|---|:---:|---|
| `contract_version` | string | ✓ | Always `"1"` |
| `worker_id` | string | ✓ | Unique per process; hub indexes freshness by this key |
| `service`, `queue_group`, `subject`, `host`, `ts` | string/number | ✓ | Populated by `NatsAdapterBase.heartbeat_payload` |
| `model_loaded` | string | — | Active model/engine name |
| `vram_used_mb`, `vram_total_mb` | integer | — | `0` when pynvml is unavailable |
| `active_requests` | integer | — | In-flight count for capacity signalling |

**Cadence.** Satellites emit a heartbeat every **5 s**. The hub marks a worker alive if a heartbeat landed within the last **15 s** (`_HB_TTL`); older entries are pruned after `2 × _HB_TTL`. No worker alive → `STTUnavailableError("STT: no live worker (heartbeat stale >15s)")` / `TtsUnavailableError` before any request is dispatched.

A heartbeat with a missing `worker_id` is dropped with a warning log and does not update freshness.

### Image Contract (from ADR-050)

#### Subjects

| Subject | Type | Direction | Producer | Consumer | Purpose |
|---|---|---|---|---|---|
| `lyra.image.generate.request` | request-reply | hub → satellite | `NatsImageClient` (hub) | `ImageNatsAdapter.handle` (satellite) | Generate image from prompt |
| `lyra.image.heartbeat` | fire-and-forget | satellite → hub | `ImageNatsAdapter` (satellite) | `NatsImageClient._on_heartbeat` (hub) | Liveness + capacity |

Queue group (load balancing across satellite replicas): `IMAGE_WORKERS`. Replies use the NATS request-reply inbox; no dedicated reply subject.

#### Payload encoding

UTF-8 JSON. Binary image data is base64-encoded in-payload (`image_b64`) or, when the encoded length exceeds the satellite's conservative ceiling, replaced by an absolute `file_path` on a shared filesystem. NATS `max_payload` is set to **50 MB** (`52428800`) in `deploy/nats/nats-container.conf`, covering audio, images, and video payloads across all domains. The satellite enforces a **750 KB base64 ceiling** independently of the server cap (see Max payload section below).

#### `contract_version` — additive, one-way

Every payload listed below carries `contract_version: "1"` as a top-level string field.

- **Producers (hub or satellite) MUST emit it** on every outgoing request, reply, and heartbeat.
- **Consumers MUST ignore unknown values.** A consumer reading a payload uses `.get("contract_version")` (or simply does not read it at all); it MUST NOT reject or error on any value. `"1"`, `"2"`, `"whatever"`, or missing — all are accepted.
- The field is **additive**, not a handshake. A `"2"` sender and a `"1"` reader continue to work as long as no other field has been removed or reinterpreted between versions. When an incompatible change is needed, it ships as a new subject (or a new ADR), not a bumped version.
- The goal is **observability** — logs and metrics on both sides can filter by version, and operators can diagnose mismatches on the wire without guessing.

#### Request — `lyra.image.generate.request`

```json
{
  "contract_version": "1",
  "trace_id": "7f3a9c12-1b4e-4e8a-a2d5-0f9e2c3b1a7d",
  "issued_at": "2026-04-19T14:32:00.123456+00:00",
  "request_id": "a1b2c3d4-e5f6-4a7b-8c9d-0e1f2a3b4c5d",
  "prompt": "a serene mountain lake at golden hour, photorealistic",
  "engine": "flux2-klein",
  "negative_prompt": "blurry, oversaturated",
  "width": 1024,
  "height": 1024,
  "steps": 50,
  "guidance": 4.0,
  "seed": 42,
  "format": "png",
  "output_mode": "b64",
  "lora_path": null,
  "lora_scale": null,
  "trigger": null,
  "embedding_path": null
}
```

| Field | Type | Required | Notes |
|---|---|:---:|---|
| `contract_version` | string | ✓ | Always `"1"` for this ADR |
| `trace_id` | string (uuid4) | ✓ | End-to-end trace correlation; emitted by hub, echoed in reply |
| `issued_at` | string (ISO-8601 datetime) | ✓ | UTC timestamp of request creation |
| `request_id` | string (uuid4) | ✓ | Unique per request; echoed back in all replies |
| `prompt` | string | ✓ | User-supplied generation prompt; see sanitization contract |
| `engine` | string | ✓ | Engine name (e.g. `flux2-klein`); validated against satellite registry |
| `negative_prompt` | string \| null | — | Negative conditioning; forwarded verbatim with length cap |
| `width` | integer \| null | — | Output width in pixels; satellite enforces ≤ 4096 |
| `height` | integer \| null | — | Output height in pixels; satellite enforces ≤ 4096 |
| `steps` | integer \| null | — | Denoising steps; satellite enforces ≤ 200 |
| `guidance` | float \| null | — | Classifier-free guidance scale |
| `seed` | integer \| null | — | RNG seed for reproducibility; null → random |
| `format` | enum `png` \| `jpeg` \| `webp` | — | Output image format; default `png` |
| `output_mode` | enum `b64` \| `file` | — | Preferred delivery mode; default `b64`; satellite may downgrade to `file` on size |
| `lora_path` | string \| null | — | Absolute path to LoRA weights; satellite enforces allowlist |
| `lora_scale` | float \| null | — | LoRA conditioning scale |
| `trigger` | string \| null | — | LoRA trigger word; forwarded verbatim with length cap |
| `embedding_path` | string \| null | — | Absolute path to textual embedding; satellite enforces allowlist |

#### Reply — success

```json
{
  "contract_version": "1",
  "trace_id": "7f3a9c12-1b4e-4e8a-a2d5-0f9e2c3b1a7d",
  "issued_at": "2026-04-19T14:32:03.456789+00:00",
  "request_id": "a1b2c3d4-e5f6-4a7b-8c9d-0e1f2a3b4c5d",
  "ok": true,
  "image_b64": "iVBORw0KGgoAAAANSUhEUgAA...",
  "mime_type": "image/png",
  "width": 1024,
  "height": 1024,
  "engine": "flux2-klein",
  "seed_used": 42
}
```

| Field | Type | Required | Notes |
|---|---|:---:|---|
| `contract_version` | string | ✓ | Always `"1"` |
| `trace_id` | string | ✓ | Echoed from request |
| `issued_at` | string | ✓ | UTC timestamp of reply creation |
| `request_id` | string | ✓ | Echoed from request |
| `ok` | boolean | ✓ | Always `true` on success path |
| `image_b64` | string (base64) | — | Mutually exclusive with `file_path`; present when encoded size ≤ 750 KB |
| `file_path` | string | — | Mutually exclusive with `image_b64`; absolute satellite-resolved path; present when `output_mode: "file"` or base64 ceiling exceeded |
| `mime_type` | string | ✓ | e.g. `image/png`, `image/jpeg`, `image/webp` |
| `width` | integer | ✓ | Actual output width |
| `height` | integer | ✓ | Actual output height |
| `engine` | string | ✓ | Engine that produced the image |
| `seed_used` | integer | ✓ | Actual seed used; 0 when satellite cannot extract it from the engine |

Exactly one of `image_b64` or `file_path` MUST be present on a success reply. Both present or both absent is a malformed reply.

#### Reply — error

```json
{
  "contract_version": "1",
  "trace_id": "7f3a9c12-1b4e-4e8a-a2d5-0f9e2c3b1a7d",
  "issued_at": "2026-04-19T14:32:01.000000+00:00",
  "request_id": "a1b2c3d4-e5f6-4a7b-8c9d-0e1f2a3b4c5d",
  "ok": false,
  "error": "unknown_engine",
  "error_detail": "flux99-nonexistent"
}
```

| Field | Type | Required | Notes |
|---|---|:---:|---|
| `contract_version` | string | ✓ | Always `"1"` |
| `trace_id` | string | ✓ | Echoed from request |
| `issued_at` | string | ✓ | UTC timestamp of reply creation |
| `request_id` | string | ✓ | Echoed from request |
| `ok` | boolean | ✓ | Always `false` on error path |
| `error` | string | ✓ | Snake_case error code; see error codes table |
| `error_detail` | string \| null | — | Optional human-readable detail; sanitized — MUST NOT leak internal paths, stack traces, or model internals |

#### Error codes

| Code | Trigger | Notes |
|---|---|---|
| `missing_required_field` | `prompt` or `engine` absent; `width`/`height` > 4096; `steps` > 200; `lora_path` or `embedding_path` outside allowlist | Consolidates validation failures. Tokens are for logs and metrics, not control flow. |
| `unknown_engine` | Engine name not found in satellite registry | |
| `engine_load_failed` | Engine import error or initialization failure | e.g. missing torch extension |
| `insufficient_resources` | Preflight VRAM/RAM check fails; `InsufficientResourcesError` or `MemoryError` during load | |
| `generation_failed` | Runtime error during image generation or response encoding | Catch-all for unclassified failures |

The hub raises `ImageUnavailableError` (or equivalent) on `ok: false` regardless of the specific `error` value — the token is for logs and metrics, not for branching control flow.

#### Heartbeat — `lyra.image.heartbeat`

```json
{
  "contract_version": "1",
  "trace_id": "00000000-0000-0000-0000-000000000000",
  "issued_at": "2026-04-19T14:32:10.000000+00:00",
  "worker_id": "image-roxabituwer-1713531130",
  "service": "image_workers",
  "host": "roxabituwer",
  "subject": "lyra.image.generate.request",
  "queue_group": "IMAGE_WORKERS",
  "ts": 1713531130.0,
  "engine_loaded": "flux2-klein",
  "active_requests": 0,
  "vram_used_mb": 8192,
  "vram_total_mb": 10240
}
```

| Field | Type | Required | Notes |
|---|---|:---:|---|
| `contract_version` | string | ✓ | Always `"1"` |
| `trace_id` | string | ✓ | Populated by `NatsAdapterBase.heartbeat_payload`; zero UUID is acceptable for heartbeats |
| `issued_at` | string | ✓ | UTC timestamp of heartbeat emission |
| `worker_id` | string | ✓ | Unique per process; hub indexes freshness by this key; drop heartbeat if absent |
| `service` | string | ✓ | Populated by `NatsAdapterBase.heartbeat_payload` |
| `host` | string | ✓ | Populated by `NatsAdapterBase.heartbeat_payload` |
| `subject` | string | ✓ | Always `lyra.image.generate.request` |
| `queue_group` | string | ✓ | Always `IMAGE_WORKERS` |
| `ts` | float | ✓ | Unix timestamp (seconds) of emission |
| `engine_loaded` | string \| null | — | Currently loaded engine name; null if no engine warmed |
| `active_requests` | integer \| null | — | In-flight request count; hub uses this for load-picking when multiple workers are alive |
| `vram_used_mb` | integer | — | **Optional** — satellite may not populate if pynvml is unavailable; hub MUST NOT treat absence as an error |
| `vram_total_mb` | integer | — | **Optional** — same caveat as `vram_used_mb` |

**Cadence.** Satellites emit a heartbeat every **5 s** (`heartbeat_interval=5.0` in `ImageNatsAdapter.__init__`). The hub marks a worker alive if a heartbeat landed within the last **15 s** (`_HB_TTL`); older entries are pruned after `2 × _HB_TTL`. No worker alive → hub raises an error before dispatching any request.

A heartbeat with a missing `worker_id` is dropped with a warning log and does not update freshness. The hub uses `active_requests` (not `vram_used_mb`) as the primary load-picking signal — VRAM telemetry is informational.

#### Max payload (ADR-047 Rule 4 compliance)

The NATS server is configured with `max_payload: 52428800` (50 MB) in `deploy/nats/nats-container.conf`. This cap covers audio, images, and video traffic across all lyra domains.

ADR-047 Rule 4 states: "Default 1 MB. Domains with legitimate binary payloads (audio, image) MUST declare the cap in their contract ADR." This ADR hereby declares the 50 MB server cap for the image domain.

The satellite additionally enforces a **conservative base64 ceiling of 750 KB** in `imagecli/src/imagecli/nats/adapter.py:254–310`:

- Base64 overhead is approximately 4/3, so 750 KB of base64 corresponds to roughly 562 KB of raw image bytes.
- When the encoded length exceeds 750 KB, the satellite automatically downgrades to `file_path` mode regardless of the requested `output_mode`. It writes the image to `images/images_out/nats_{request_id[:8]}.{fmt}` relative to the satellite CWD, then replies the absolute path from `Path.resolve()`.
- `file_path` mode requires a **shared filesystem** between hub and worker. This holds today — hub and imageCLI satellite both run on Machine 1 (`roxabituwer`). Any future hub/worker split to separate hosts will require replacing the `file_path` fallback with an alternative binary transfer mechanism. This dependency is flagged as a known architectural constraint.
- The hub MUST treat `file_path` as satellite-resolved and hub-opaque: no validation, no traversal, no path normalization. The hub reads the file at the given path to deliver the image to the user.

#### User-controlled fields (sanitization contract)

The satellite (`imagecli/src/imagecli/nats/adapter.py:66–129`) owns sanitization of all user-supplied content. The hub forwards fields verbatim; the hub MUST NOT attempt to sanitize satellite inputs.

| Field | Trust level | Sanitization owner | Enforcement |
|---|---|---|---|
| `prompt` | Untrusted (user input) | Satellite | Engine receives as-is; length cap applied before dispatch |
| `negative_prompt` | Untrusted (user input) | Satellite | Same as `prompt` |
| `trigger` | Untrusted (user input) | Satellite | Same as `prompt`; passed to engine as LoRA trigger word |
| `lora_path` | Untrusted (user input) | Satellite | MUST be absolute; MUST resolve under `~/ComfyUI/models/{loras,lora}`; validated in `_validate_path` → `missing_required_field` on violation |
| `embedding_path` | Untrusted (user input) | Satellite | MUST be absolute; MUST resolve under `~/ComfyUI/models/embeddings`; validated in `_validate_path` → `missing_required_field` on violation |
| `width` | Untrusted (user input) | Satellite | MUST be ≤ 4096; validated in `_validate_request` → `missing_required_field` on violation |
| `height` | Untrusted (user input) | Satellite | MUST be ≤ 4096; same enforcement |
| `steps` | Untrusted (user input) | Satellite | MUST be ≤ 200; same enforcement |

Path validation uses `Path.resolve()` to canonicalize before the `relative_to` check — symlinks and `..` traversal attempts are blocked. The satellite error detail MUST NOT leak the resolved path or the allowlist directory structure to the caller; sanitized messages only.

### Relationship to existing ADRs

| ADR | Before this ADR | After this ADR |
|---|---|---|
| ADR-044 (voice contract) | Standalone ADR defining voice contract | **Superseded** — Voice contract now defined in §Voice Contract (from ADR-044) below; `roxabi_contracts.voice.models` is the normative source |
| ADR-045 (roxabi-nats) | Transport + contract commingled in `adapter_base.py` (`CONTRACT_VERSION` constant) | Transport in `roxabi-nats`; per-domain contract in `roxabi-contracts`; `CONTRACT_VERSION` moves to `roxabi_contracts.envelope` |
| ADR-046 (nkey per-worker ACL) | Unaffected | Unaffected — auth remains at the NATS subject layer, not the schema layer. See §Trust Model below |
| ADR-047 (worker ownership) | Lyra owns subjects + publishers + nkey; satellites own workers | Unchanged. Subject string literals move from satellite-side constants to `roxabi_contracts.<domain>.SUBJECTS` — still authored in lyra-controlled code, now imported by satellites. Grep-gate updated to confine `import roxabi_contracts.<domain>` to the satellite's designated adapter module |
| ADR-048 (infrastructure layer) | Unaffected | Unaffected — contracts are wire, not persistence |
| ADR-050 (image contract) | Standalone ADR defining image contract | **Superseded** — Image contract now defined in §Image Contract (from ADR-050) below; `roxabi_contracts.image.models` is the normative source |

### Dependency arrows

```
roxabi-nats (transport, zero lyra knowledge)
   ↑
   └── roxabi-contracts (pure Pydantic at runtime; roxabi-nats only via [testing] extra)
          ↑                              ↑
          │                              │
          lyra (publishers + [testing])  satellites (workers + [testing] in dev)
                                         voiceCLI, imageCLI, roxabi-vault
```

**Critical boundary:** `roxabi-contracts` runtime has zero transport dependency. A satellite that only needs schemas + fixtures installs `roxabi-contracts` without pulling `roxabi-nats` or `nats-py`. The `[testing]` extra is the only path that brings in transport — and only for test-double consumers.

### Versioning

Independent from both `roxabi-nats` and ADR-044 `contract_version`. Tag scheme: `roxabi-contracts/v{major}.{minor}.{patch}`.

| Change type | Example | Bump |
|---|---|---|
| Add optional field (non-security) | `TtsRequest.style` (defaults to None) | minor |
| Add a new domain submodule | `roxabi_contracts.memory` | minor |
| Add a new subject to an existing domain | `SUBJECTS.tts_stream` | minor |
| Rename/retype a required field | `text` → `prompt` on `TtsRequest` | major **and** new `contract_version` per ADR-044 |
| Remove a deprecated field | Drop `sample_rate` after grace period | major |
| **Add a security-bearing field** | caller identity, auth scopes, signed token | **major + new `contract_version`** — MUST NOT be additive |

**Forward-compat invariant:** all `ContractEnvelope` subclasses declare `model_config = ConfigDict(extra="ignore")`. A v0.1.0 satellite receiving a v0.2.0 payload with a new optional field parses cleanly — unknown fields are dropped without error. Enforced by `test_voice_extra_ignore.py`.

**Security-bearing fields exclusion:** any field whose presence or value affects authorization, identity attestation, or audit provenance is **not** eligible for additive introduction. A v1 satellite silently ignoring an `auth_token` or `caller_nkey` field adds the class of bug where the hub believes it shipped a tightened check but the satellite never enforced it. Such fields require a major bump + coordinated satellite upgrade + ADR amendment. Each domain contract ADR must list which fields (if any) are security-bearing.

### Trust Model

`roxabi-contracts` performs **structural validation only** — Pydantic enforces types, required vs optional, basic constraints (min/max length, regex patterns where declared). It does **not** sanitize content. The following remain the satellite worker's responsibility, per ADR-047 §Rule 4:

- NUL byte stripping on any user-controlled string field
- Path normalization / traversal rejection on filename-typed fields
- Base64 length cross-check (decoded length vs claimed `sample_rate`, `duration`, etc.)
- Domain-specific content rules (allowed voice IDs, model names, prompt length caps)

**Consumers MUST NOT call `Model.model_validate_json()` directly on raw NATS `msg.data`.** The correct call site is `roxabi_nats.deserialize()`, which runs the pre-validation byte-size gate (default 1 MB, per truth §14.3 rule 5) before handing bytes to Pydantic. Calling the Pydantic method directly bypasses the gate and allows a 50 MB base64 blob to run through the JSON parser before any limit fires.

Enforcement: `NatsAdapterBase._dispatch()` is the only caller of `deserialize()` in production paths. Test doubles in `testing.py` may call model methods directly only on pre-validated fixture data.

### Test-double pattern

Test doubles live in `roxabi_contracts.<domain>.testing` and require the `[testing]` optional extra. Three mandatory safety guards prevent production contamination:

1. **Separate extras.** `pyproject.toml` declares `[project.optional-dependencies].testing = ["nats-py>=2.6", "roxabi-nats"]`. Production installs (`uv sync` without `--extra testing`) do not pull transport deps. Importing `roxabi_contracts.voice.testing` in a prod venv fails at import time with `ModuleNotFoundError: nats`.

2. **Environment assertion.** `FakeTtsWorker.__init__` raises `RuntimeError("FakeTtsWorker cannot run in production")` if `os.environ.get("LYRA_ENV") == "production"`. No override flag, no escape hatch — adversarial CI scenarios that need the fake worker must run with a non-production `LYRA_ENV` value (e.g., `LYRA_ENV=ci_adversarial`). Security-by-obscurity flags are explicitly rejected.

3. **Loopback-only NATS URL.** `FakeTtsWorker.connect()` refuses non-loopback NATS URLs (anything other than `nats://127.0.0.1:*` or `nats://localhost:*`). Raises `ValueError` on non-loopback. No override. This closes the accidental-prod-queue-group-shadowing class of bugs even when `LYRA_ENV` is misconfigured.

The three guards are **independent and non-bypassable** — an attacker who flips one must also flip the others to shadow a production worker. The `[testing]` extra is the strongest of the three because it fails at import time, before any runtime code executes.

API surface:
```python
# Signature:
class FakeTtsWorker:
    def __init__(self, nats_url: str = "nats://127.0.0.1:4222", reply_fixture: bytes | None = None): ...
    async def start(self) -> None: ...
    async def stop(self) -> None: ...
    calls: list[TtsRequest]  # for assertions
```

Lyra's `test_nats_tts_client_e2e.py` imports `FakeTtsWorker` and drives the full publish/reply cycle without voiceCLI. Satellite-side tests in voiceCLI import `roxabi_contracts.voice.fixtures` for pure-data assertions without the transport weight.

### Consumption

**Internal (Lyra):**

```toml
[tool.uv.sources]
roxabi-nats = { workspace = true }
roxabi-contracts = { workspace = true, extras = ["testing"] }
```

**External runtime (voiceCLI, imageCLI, roxabi-vault — production deps):**

```toml
[tool.uv.sources]
roxabi-nats      = { git = "https://github.com/Roxabi/lyra.git", subdirectory = "packages/roxabi-nats",      tag = "roxabi-nats/v0.2.0" }
roxabi-contracts = { git = "https://github.com/Roxabi/lyra.git", subdirectory = "packages/roxabi-contracts", tag = "roxabi-contracts/v0.1.0" }
```

**External dev (satellites importing test doubles for their own unit tests):**

```toml
[dependency-groups]
dev = [
  { include-group = "runtime" },
  "roxabi-contracts[testing]",
]
```

Both tags are independent. A voiceCLI release can pin a specific `roxabi-contracts` version without forcing an image or memory contract update.

### Release engineering

**release-please ownership.** The root lyra `release-please-config.json` currently covers only `.`. Add both subpackages:

```json
{
  "packages": {
    ".": { "release-type": "python", "package-name": "lyra" },
    "packages/roxabi-nats": {
      "release-type": "python",
      "component": "roxabi-nats",
      "tag-separator": "/",
      "package-name": "roxabi-nats"
    },
    "packages/roxabi-contracts": {
      "release-type": "python",
      "component": "roxabi-contracts",
      "tag-separator": "/",
      "package-name": "roxabi-contracts"
    }
  }
}
```

Note: release-please does not support a `tag-name` template field — tags are constructed from `component` + `tag-separator` + (optional `v`) + version. The `tag-separator: "/"` above yields `roxabi-nats/v0.1.0` (with the default `include-v-in-tag: true`), which is the slash-path scheme this ADR relies on for independent subpackage tags.

Seed `.release-please-manifest.json` with `"packages/roxabi-nats": "0.1.0"` and `"packages/roxabi-contracts": "0.1.0"` before first run so release-please does not attempt a root-lyra bump from the subpackage commits.

**Release coordination — actor and sequence.** Lyra maintainer is the release-owner for both subpackages. Cross-repo rollout sequence is strictly ordered to avoid broken satellite lockfiles:

1. Lyra PR merges (new subpackage + updated publishers + `uv.lock` regenerated).
2. `roxabi-contracts/v0.1.0` tag is cut on the merge commit (release-please or manual).
3. `roxabi-nats/v0.2.0` tag is cut in the same release cycle if the `CONTRACT_VERSION` migration bumps it.
4. Release notes announce tags on Discord / release channel.
5. Satellite PRs (voiceCLI#69, imageCLI adoption, vault) open **after** step 2 — they reference the now-existing tag and cannot land before it.

voiceCLI#69 must not merge before the tag exists — its `uv sync` would fail resolving the phantom tag. This reverses the earlier Phase 1 wording that implied simultaneity.

**Satellite pin freshness.** Each satellite repo adds a Renovate (or Dependabot) rule:

```json5
// renovate.json — satellite repos
{
  "packageRules": [{
    "matchDatasources": ["git-refs"],
    "matchSourceUrls": ["https://github.com/Roxabi/lyra"],
    "matchPackageNames": ["roxabi-nats", "roxabi-contracts"],
    "groupName": "roxabi sdk",
    "schedule": ["before 6am on monday"]
  }]
}
```

`matchDatasources: ["git-refs"]` is required — Renovate resolves git-sourced `uv` pins through the `git-refs` datasource, not the default `pypi` datasource. Without it, the rule silently does not fire.

Renovate reads the `tag = "..."` pin and opens a PR when a new tag appears. Without this, pin freshness is purely documentation and drift becomes inevitable.

### Python version floor

Matches `roxabi-nats`: `requires-python = ">=3.12"`, broader than lyra's narrower pin. Lets satellites track a newer interpreter than the hub.

### Test layout and CI integration

Tests live at `packages/roxabi-contracts/tests/`. The current CI pattern is a single merged `uv run pytest` step with `testpaths = ["tests", "packages/roxabi-nats/tests"]`. Extend this to include `packages/roxabi-contracts/tests`:

```toml
# root pyproject.toml
[tool.pytest.ini_options]
testpaths = ["tests", "packages/roxabi-nats/tests", "packages/roxabi-contracts/tests"]
addopts  = "... --cov=roxabi_contracts ..."
```

A hub test failure continues to fail the same job as subpackage tests — this is the existing pattern and not a regression from ADR-049. Matrix split across subpackages is out of scope.

### Pre-requisites

| Issue | Blocker? | Reason |
|---|---|---|
| ADR-045 merged + `roxabi-nats/v0.1.0` tagged | Yes | Already done. |
| ADR-047 merged | Yes | Establishes subject ownership. Already done. |
| voiceCLI#69 not yet merged | Yes | Must open against `roxabi-contracts/v0.1.0` tag, not pre-empt it. |
| `lyra.image.*` contract ADR | No | Blocks Phase 2 only. |

### Migration plan

Phase 1 — bootstrap the package and port voice:

1. Create `packages/roxabi-contracts/` skeleton + `envelope.py` + `voice/` submodule with `subjects.py`, `models.py` (all with `ConfigDict(extra="ignore")`), `fixtures.py` (synthetic data only — see §Fixture generation).
2. Port `TtsRequest` / `TtsResponse` / `SttRequest` / `SttResponse` dataclasses from lyra + voiceCLI into typed Pydantic models. Reconcile any silent drift as part of the port.
3. Extract `SUBJECTS` namespace from `src/lyra/nats/nats_tts_client.py` and voiceCLI's `tts_adapter.py`.
4. Ship `testing.py` with `FakeTtsWorker` + the three production guard-rails (`[testing]` extra, `LYRA_ENV` assertion, loopback-only URL check).
5. Update lyra `NatsTtsClient` / `NatsSttClient` to import from `roxabi_contracts.voice`. Migrate `CONTRACT_VERSION` from `roxabi_nats.adapter_base` to `roxabi_contracts.envelope` and re-export from `roxabi_nats` for one release as a compat shim.
6. Add `packages/roxabi-contracts` to the root `pyproject.toml`:
   - `[tool.uv.workspace].members += ["packages/roxabi-contracts"]`
   - `[tool.pyright].include += ["packages/roxabi-contracts/src"]`
   - `[tool.ruff].src += ["packages/roxabi-contracts/src"]`
   - `[tool.pytest.ini_options].testpaths += ["packages/roxabi-contracts/tests"]`
7. Update `release-please-config.json` with the `packages/roxabi-contracts` entry. Seed `.release-please-manifest.json` with `"packages/roxabi-contracts": "0.1.0"`.
8. Regenerate and commit `uv.lock` (without this, `uv sync --frozen` on `roxabituwer` fails at `deploy.sh:45`).
9. Merge the lyra PR. Cut `roxabi-contracts/v0.1.0` tag on the merge commit.
10. voiceCLI#69 opens against the newly-existing tag, adopts both `roxabi-nats v0.2.0` and `roxabi-contracts v0.1.0` in the same PR.

Phase 2 — image:

1. Land the pending `lyra.image.*` contract ADR.
2. Add `roxabi_contracts.image/` submodule (models, subjects, fixtures, testing).
3. Lyra ships `NatsImageClient` publisher.
4. imageCLI's satellite-side subscriber (already shipped per truth §14.1) switches to the contract import.
5. Tag `roxabi-contracts/v0.2.0`.

Phase 3 — memory + llm:

1. Write contract ADRs for each domain.
2. Add submodules.
3. Wire publishers and subscribers.

### Fixture generation

All binary fixtures (WAV, PNG, etc.) MUST be synthetically generated inside `fixtures.py` using standard libraries (`scipy.io.wavfile.write` for audio, `PIL` for images) — never seeded from real user data or live-session captures. Rationale: open-source package must not ship any byte that could leak user audio, API keys, hostnames (RIFF vendor strings, ID3 tags), or model outputs used as dev seeds.

CI check: a `tests/test_fixture_provenance.py` asserts each fixture is byte-identical to the output of its documented generator call. If `silence_wav_16khz` is declared as "1 second of silence at 16kHz, 16-bit PCM," the test regenerates it and compares — any drift from the canonical generator output fails CI.

### Out of scope

- **Code generation (protobuf, OpenAPI, JSON Schema export).** Pydantic is the authored form. If a non-Python consumer ever materializes, a generator can emit schemas from the Pydantic models — not planned now.
- **Versioned envelope negotiation.** `contract_version` is a single string field; rollback or multi-version parsing on the subscriber side is not added by this ADR. When `contract_version: "2"` ships, it is a coordinated major bump per ADR-044.
- **Non-Roxabi consumers.** Same PyPI deferral criteria as ADR-045 §PyPI migration criteria apply here.
- **Replacing per-satellite subscriber code.** Workers still live in satellite repos per ADR-047. This ADR only standardizes the *schema* they subscribe against.
- **Matrix-split CI.** Subpackage tests continue to run in the shared pytest job as they do for `roxabi-nats` today.

## Consequences

### Positive

- Drift between lyra publisher and satellite subscriber becomes a type-check error, not a silent wire mismatch.
- E2E tests run in-process on either side. Laptop development no longer requires a GPU.
- New domains (memory, llm) have a well-known place to land — no ad-hoc per-repo envelope re-implementation.
- The microkernel boundary is explicit in code: kernel + transport + contract + plugin are four separate coordinates, each with its own version.
- voiceCLI#69 adoption becomes one PR with two dependency updates.
- Satellites that need only schemas install no transport code; `[testing]` extras stay out of production venvs.

### Negative

- Three version coordinates (`lyra`, `roxabi-nats`, `roxabi-contracts`) plus wire `contract_version`. Release-engineering discipline is load-bearing; release-please + Renovate mitigate but do not eliminate.
- Satellite pinned to an old `roxabi-contracts` can block a lyra envelope evolution until it upgrades. Same coordination tax as ADR-044 prose, now machine-enforceable.
- Pydantic v2 becomes a mandatory runtime dep for every satellite (~8 MB compiled core). No current satellite has a conflicting pin, but install-size is non-zero.

### Neutral

- `CONTRACT_VERSION` constant migrates from `roxabi_nats.adapter_base` to `roxabi_contracts.envelope`. A compat re-export stays in `roxabi_nats` for one release cycle (until `roxabi-nats/v0.3.0`), then is removed. The `roxabi-nats/v0.3.0` release MUST carry a `BREAKING CHANGE:` trailer in its Conventional Commit so release-please surfaces the removal prominently in the changelog and so Renovate opens a major-bump PR in each satellite — any external pin on `roxabi_nats.CONTRACT_VERSION` breaks at that point and requires a source update.
- ADR-044 remains the human-readable voice contract; it gains a canonical link to `roxabi_contracts.voice`. Future contract ADRs follow the same pattern.
- Subject constants move from scattered `f"lyra.voice.tts.request"` literals into `SUBJECTS.tts_request`. Grep-gate in each satellite (ADR-047 §14.3) is updated: the check shifts from "no `lyra.*` string literal outside adapter module" to "no `import roxabi_contracts.<domain>` outside the designated adapter module." One PR per satellite to update the gate configuration.
