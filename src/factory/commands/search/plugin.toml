name = "search"
description = "Full-text search over the vault"
version = "0.1.0"
priority = 100
enabled = true
timeout = 30.0

# /search is registered as a session command by agents (via register_session_command)
# to receive SessionTools injection. No plugin commands exported from this module.
