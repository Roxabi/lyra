### Summary
No issues found in the 16 files analyzed for partition P4 (processors, auth). All async code correctly uses `await` for coroutines, offloads blocking DNS lookups via `asyncio.to_thread`, and avoids event-loop misuse. The auth layer is purely synchronous and reads only from in-memory caches (no blocking I/O in async contexts).

### Findings
| severity | file | line | description |
|---|------|------|-------------|

*(none)*

### Metrics
- files_audited: 16
- issues_found: 0
- P0: 0 | P1: 0 | P2: 0 | P3: 0

### Recommendations
1. **Clean** — `src/lyra/core/processors/_scraping.py` correctly offloads `socket.getaddrinfo` to `asyncio.to_thread` (line 39). No action needed.
2. **Clean** — `src/lyra/core/processors/stream_processor.py` handles iterator cleanup via `await _aclose()` in `finally` (line 217). No action needed.
3. **Clean** — `src/lyra/core/auth/authenticator.py` relies on cache-only synchronous reads from `AuthStore` and `IdentityAliasStore`. No action needed.
