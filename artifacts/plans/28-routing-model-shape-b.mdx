---
title: "Plan: Slice 1 — Routing model (Shape B)"
issue: 28
parent: 15
spec: artifacts/specs/15-telegram-discord-hub-adapters.mdx
complexity: 5/10
tier: F-lite
generated: 2026-03-03
---

## Summary

Replace `BindingKey(channel, user_id)` with `RoutingKey(platform, bot_id, user_id)` and add
`Platform` enum, `TelegramContext`/`DiscordContext`, and the full hub routing pipeline
(`get_or_create_pool`, `dispatch_response`, `run()`). Migrate all 21 existing tests and add
new ones covering the new API. Atomic: `dispatch_response` migration ships in the same commit
as `RoutingKey` introduction (ADR-001 requirement).

## Agents

| Agent | Tasks | Files |
|-------|-------|-------|
| backend-dev | 8–19 | `src/lyra/core/message.py`, `src/lyra/core/hub.py`, `src/lyra/core/__init__.py` |
| tester | 1–7 | `tests/core/test_hub.py` |

## Reference Patterns

- `src/lyra/core/hub.py:23` — existing `BindingKey(NamedTuple)` pattern → model `RoutingKey` on it
- `src/lyra/core/pool.py:9` — `@dataclass(frozen=True)` pattern → use for `TelegramContext`, `DiscordContext`
- `docs/architecture/adr/001-routing-key-platform-bot-id-user-id.mdx` — ADR mandating atomic dispatch_response migration

## Consistency Report

| SC | Covered by task(s) |
|----|-------------------|
| SC-1: RoutingKey replaces BindingKey, all tests pass | T3, T11, T14, T19 |
| SC-2: Message carries 6 new fields | T1, T2, T8, T9, T10 |
| SC-3: adapter_registry dict[(Platform, str), …] | T4, T12 |
| SC-4: Pool ID format f"{platform}:{bot_id}:{user_id}" | T7, T15 |
| SC-5: Unmatched RoutingKey → log warning, no exception | T6, T17 |
| SC-6: dispatch_response uses (platform, bot_id), atomic | T5, T16 |

## Micro-Tasks

### Phase RED — Failing tests (tester)

---

**T1** — Update `make_message()` helper and `TestMessage` to new `Message` API
- **File:** `tests/core/test_hub.py`
- **Snippet:**
  ```python
  def make_message(
      platform: Platform = Platform.TELEGRAM,
      bot_id: str = "main",
      user_id: str = "alice",
  ) -> Message:
      return Message(
          id="msg-1",
          platform=platform,
          bot_id=bot_id,
          channel=platform.value,   # deprecated alias
          user_id=user_id,
          user_name="Alice",
          is_mention=False,
          is_from_bot=False,
          content="hello",
          type=MessageType.TEXT,
          timestamp=datetime.now(timezone.utc),
          platform_context=TelegramContext(chat_id=42),
      )
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestMessage -x` → RED (ImportError on Platform)
- **Spec trace:** SC-2
- **Phase:** RED | **Difficulty:** 2 | **[P] parallel-safe: N** (shared helper)

---

**T2** — Add `TestPlatformContext` — frozen dataclass fields, defaults
- **File:** `tests/core/test_hub.py`
- **Snippet:**
  ```python
  class TestPlatformContext:
      def test_telegram_context_fields(self) -> None:
          ctx = TelegramContext(chat_id=123)
          assert ctx.chat_id == 123
          assert ctx.topic_id is None
          assert ctx.is_group is False

      def test_discord_context_fields(self) -> None:
          ctx = DiscordContext(guild_id=1, channel_id=2, message_id=3)
          assert ctx.thread_id is None
          assert ctx.channel_type == "text"

      def test_contexts_are_frozen(self) -> None:
          ctx = TelegramContext(chat_id=1)
          with pytest.raises((AttributeError, TypeError)):
              ctx.chat_id = 99  # type: ignore[misc]
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestPlatformContext -x` → RED
- **Spec trace:** SC-2
- **Phase:** RED | **Difficulty:** 1 | **[P] parallel-safe: Y**

---

**T3** — Update `TestBindings` → `TestRoutingKey` with new API
- **File:** `tests/core/test_hub.py`
- **Snippet:**
  ```python
  class TestRoutingKey:
      def test_exact_binding(self) -> None:
          hub = Hub()
          hub.register_adapter(Platform.TELEGRAM, "main", MockAdapter())
          hub.register_binding(Platform.TELEGRAM, "main", "alice", "lyra", "telegram:main:alice")
          msg = make_message(platform=Platform.TELEGRAM, user_id="alice")
          binding = hub.resolve_binding(msg)
          assert binding is not None
          assert binding.pool_id == "telegram:main:alice"

      def test_wildcard_does_not_bleed_across_platforms(self) -> None:
          hub = Hub()
          hub.register_binding(Platform.DISCORD, "main", "*", "lyra", "discord:main:*")
          msg = make_message(platform=Platform.TELEGRAM, user_id="unknown")
          assert hub.resolve_binding(msg) is None

      def test_wildcard_fallback_same_platform(self) -> None:
          hub = Hub()
          hub.register_binding(Platform.TELEGRAM, "main", "*", "lyra", "telegram:main:*")
          msg = make_message(platform=Platform.TELEGRAM, user_id="unknown")
          binding = hub.resolve_binding(msg)
          assert binding is not None
          assert binding.agent_name == "lyra"

      def test_no_binding_returns_none(self) -> None: ...
      def test_overwrite_binding(self) -> None: ...
      def test_binding_isolated_per_bot_id(self) -> None: ...
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestRoutingKey -x` → RED
- **Spec trace:** SC-1
- **Phase:** RED | **Difficulty:** 2 | **[P] parallel-safe: Y**

---

**T4** — Add `TestHubAdapterRegistry` — keyed by `(Platform, str)` tuple
- **File:** `tests/core/test_hub.py`
- **Snippet:**
  ```python
  class TestHubAdapterRegistry:
      def test_registers_by_platform_and_bot_id(self) -> None:
          hub = Hub()
          a = MockAdapter()
          hub.register_adapter(Platform.TELEGRAM, "main", a)
          assert hub.adapter_registry[(Platform.TELEGRAM, "main")] is a

      def test_two_bots_same_platform_no_collision(self) -> None:
          hub = Hub()
          a1, a2 = MockAdapter(), MockAdapter()
          hub.register_adapter(Platform.TELEGRAM, "bot1", a1)
          hub.register_adapter(Platform.TELEGRAM, "bot2", a2)
          assert hub.adapter_registry[(Platform.TELEGRAM, "bot1")] is a1
          assert hub.adapter_registry[(Platform.TELEGRAM, "bot2")] is a2
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestHubAdapterRegistry -x` → RED
- **Spec trace:** SC-3
- **Phase:** RED | **Difficulty:** 1 | **[P] parallel-safe: Y**

---

**T5** — Add `TestDispatchResponse` — adapter lookup by `(platform, bot_id)`
- **File:** `tests/core/test_hub.py`
- **Snippet:**
  ```python
  class TestDispatchResponse:
      async def test_dispatches_to_correct_adapter(self) -> None:
          hub = Hub()
          sent: list[tuple[Message, Response]] = []

          class CapturingAdapter:
              async def send(self, msg: Message, resp: Response) -> None:
                  sent.append((msg, resp))

          hub.register_adapter(Platform.TELEGRAM, "main", CapturingAdapter())
          msg = make_message(platform=Platform.TELEGRAM, bot_id="main")
          response = Response(content="pong")
          await hub.dispatch_response(msg, response)
          assert len(sent) == 1
          assert sent[0][1].content == "pong"

      async def test_missing_adapter_raises(self) -> None:
          hub = Hub()
          msg = make_message(platform=Platform.TELEGRAM, bot_id="ghost")
          with pytest.raises(KeyError):
              await hub.dispatch_response(msg, Response(content="x"))
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestDispatchResponse -x` → RED
- **Spec trace:** SC-6
- **Phase:** RED | **Difficulty:** 2 | **[P] parallel-safe: Y**

---

**T6** — Add `TestUnmatchedRouting` — log warning, no exception, loop continues
- **File:** `tests/core/test_hub.py`
- **Snippet:**
  ```python
  import logging

  class TestUnmatchedRouting:
      async def test_unmatched_key_logs_warning(self, caplog: pytest.LogCaptureFixture) -> None:
          hub = Hub()
          msg = make_message(platform=Platform.TELEGRAM, user_id="nobody")
          with caplog.at_level(logging.WARNING):
              # Put message on bus and run one iteration
              await hub.bus.put(msg)
              # run() is a loop; test the routing logic directly
              binding = hub.resolve_binding(msg)
              assert binding is None
              # Verify warning is issued by the run loop
              # (integration test of run() with timeout)
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestUnmatchedRouting -x` → RED
- **Spec trace:** SC-5
- **Phase:** RED | **Difficulty:** 3 | **[P] parallel-safe: Y**

---

**T7** — Add `TestPoolCreation` — pool_id format, two bots same user → separate pools
- **File:** `tests/core/test_hub.py`
- **Snippet:**
  ```python
  class TestPoolCreation:
      def test_pool_id_format(self) -> None:
          hub = Hub()
          pool = hub.get_or_create_pool("telegram:main:alice", "lyra")
          assert pool.pool_id == "telegram:main:alice"

      def test_two_bots_same_user_separate_pools(self) -> None:
          hub = Hub()
          p1 = hub.get_or_create_pool("telegram:bot1:alice", "lyra")
          p2 = hub.get_or_create_pool("telegram:bot2:alice", "lyra")
          assert p1 is not p2
          assert p1.pool_id != p2.pool_id

      def test_same_call_returns_same_pool(self) -> None:
          hub = Hub()
          p1 = hub.get_or_create_pool("telegram:main:alice", "lyra")
          p2 = hub.get_or_create_pool("telegram:main:alice", "lyra")
          assert p1 is p2
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestPoolCreation -x` → RED
- **Spec trace:** SC-4
- **Phase:** RED | **Difficulty:** 1 | **[P] parallel-safe: Y**

---

### RED-GATE ✗ — All 6 SC tests failing. Proceed to GREEN only after all T1-T7 are written.

---

### Phase GREEN — Implementation (backend-dev)

---

**T8** — Add `Platform` enum to `message.py`
- **File:** `src/lyra/core/message.py`
- **Snippet:**
  ```python
  class Platform(str, Enum):
      TELEGRAM = "telegram"
      DISCORD = "discord"
  ```
- **Verify:** `python -c "from lyra.core.message import Platform; print(Platform.TELEGRAM)"` → `Platform.TELEGRAM`
- **Spec trace:** SC-2
- **Phase:** GREEN | **Difficulty:** 1 | **[P] parallel-safe: N** (file dependency)

---

**T9** — Add `TelegramContext`, `DiscordContext`, `PlatformContext` to `message.py`
- **File:** `src/lyra/core/message.py`
- **Snippet:**
  ```python
  @dataclass(frozen=True)
  class TelegramContext:
      chat_id: int
      topic_id: int | None = None
      is_group: bool = False

  @dataclass(frozen=True)
  class DiscordContext:
      guild_id: int
      channel_id: int
      message_id: int
      thread_id: int | None = None
      channel_type: Literal["text", "thread", "forum", "voice"] = "text"

  PlatformContext = TelegramContext | DiscordContext
  ```
- **Verify:** `python -c "from lyra.core.message import TelegramContext, DiscordContext; TelegramContext(chat_id=1)"` → OK
- **Spec trace:** SC-2
- **Phase:** GREEN | **Difficulty:** 1 | **[P] parallel-safe: N**

---

**T10** — Update `Message` dataclass — add 6 new fields, keep `channel` as deprecated alias
- **File:** `src/lyra/core/message.py`
- **Snippet:**
  ```python
  @dataclass
  class Message:
      id: str
      platform: Platform
      bot_id: str
      channel: str           # deprecated alias for platform.value — kept until migration complete
      user_id: str
      user_name: str
      is_mention: bool
      is_from_bot: bool
      content: MessageContent | str
      type: MessageType
      timestamp: datetime
      platform_context: PlatformContext
      trust: Literal["user", "system"] = "user"
      metadata: dict = field(default_factory=dict)
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestMessage -x` → GREEN
- **Spec trace:** SC-2
- **Phase:** GREEN | **Difficulty:** 2 | **[P] parallel-safe: N**

---

**T11** — Add `RoutingKey` NamedTuple to `hub.py`, remove `BindingKey` usage
- **File:** `src/lyra/core/hub.py`
- **Snippet:**
  ```python
  class RoutingKey(NamedTuple):
      """Routing key: (platform, bot_id, user_id). Use user_id='*' for wildcard."""
      platform: Platform
      bot_id: str
      user_id: str
  ```
- **Verify:** `python -c "from lyra.core.hub import RoutingKey; from lyra.core.message import Platform; RoutingKey(Platform.TELEGRAM, 'main', 'alice')"` → OK
- **Spec trace:** SC-1
- **Phase:** GREEN | **Difficulty:** 1 | **[P] parallel-safe: N**

---

**T12** — Update `Hub.adapter_registry` type + `register_adapter` signature
- **File:** `src/lyra/core/hub.py`
- **Snippet:**
  ```python
  def __init__(self, bus_size: int = BUS_SIZE) -> None:
      self.bus: asyncio.Queue[Message] = asyncio.Queue(maxsize=bus_size)
      self.adapter_registry: dict[tuple[Platform, str], ChannelAdapter] = {}
      self.bindings: dict[RoutingKey, Binding] = {}
      self.pools: dict[str, Pool] = {}

  def register_adapter(self, platform: Platform, bot_id: str, adapter: ChannelAdapter) -> None:
      self.adapter_registry[(platform, bot_id)] = adapter
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestHubAdapterRegistry -x` → GREEN
- **Spec trace:** SC-3
- **Phase:** GREEN | **Difficulty:** 2 | **[P] parallel-safe: N**

---

**T13** — Update `register_binding` signature + collision guard (new namespace)
- **File:** `src/lyra/core/hub.py`
- **Snippet:**
  ```python
  def register_binding(
      self,
      platform: Platform,
      bot_id: str,
      user_id: str,
      agent_name: str,
      pool_id: str,
  ) -> None:
      for existing_key, existing_binding in self.bindings.items():
          if (
              existing_key.platform == platform
              and existing_key.bot_id == bot_id
              and existing_key.user_id != user_id
              and existing_binding.pool_id == pool_id
          ):
              raise ValueError(
                  f"pool_id {pool_id!r} is already bound to user_id "
                  f"{existing_key.user_id!r} on {platform}:{bot_id}. "
                  "Each pool must serve at most one user per (platform, bot_id)."
              )
      self.bindings[RoutingKey(platform, bot_id, user_id)] = Binding(
          agent_name=agent_name, pool_id=pool_id
      )
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestRoutingKey -x` → GREEN (partial)
- **Spec trace:** SC-1, SC-4
- **Phase:** GREEN | **Difficulty:** 2 | **[P] parallel-safe: N**

---

**T14** — Update `resolve_binding` — use `RoutingKey`, add wildcard fallback
- **File:** `src/lyra/core/hub.py`
- **Snippet:**
  ```python
  def resolve_binding(self, msg: Message) -> Binding | None:
      key = RoutingKey(msg.platform, msg.bot_id, msg.user_id)
      exact = self.bindings.get(key)
      if exact is not None:
          return exact
      wildcard = RoutingKey(msg.platform, msg.bot_id, "*")
      return self.bindings.get(wildcard)
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestRoutingKey -x` → GREEN (all)
- **Spec trace:** SC-1
- **Phase:** GREEN | **Difficulty:** 1 | **[P] parallel-safe: N**

---

**T15** — Add `get_or_create_pool(pool_id, agent_name)` to Hub
- **File:** `src/lyra/core/hub.py`
- **Snippet:**
  ```python
  def get_or_create_pool(self, pool_id: str, agent_name: str) -> Pool:
      if pool_id not in self.pools:
          self.pools[pool_id] = Pool(pool_id=pool_id, agent_name=agent_name)
      return self.pools[pool_id]
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestPoolCreation -x` → GREEN
- **Spec trace:** SC-4
- **Phase:** GREEN | **Difficulty:** 1 | **[P] parallel-safe: N**

---

**T16** — Add `dispatch_response(msg, response)` to Hub
- **File:** `src/lyra/core/hub.py`
- **Snippet:**
  ```python
  async def dispatch_response(self, msg: Message, response: Response) -> None:
      adapter = self.adapter_registry[(msg.platform, msg.bot_id)]
      await adapter.send(msg, response)
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestDispatchResponse -x` → GREEN
- **Spec trace:** SC-6
- **Phase:** GREEN | **Difficulty:** 1 | **[P] parallel-safe: N**

---

**T17** — Add `run()` loop — consume → route → lock → process → dispatch
- **File:** `src/lyra/core/hub.py`
- **Snippet:**
  ```python
  import logging
  log = logging.getLogger(__name__)

  async def run(self) -> None:
      """Hub bus consumer loop. Runs until cancelled."""
      while True:
          msg = await self.bus.get()
          try:
              binding = self.resolve_binding(msg)
              if binding is None:
                  log.warning(
                      "unmatched routing key %s — message dropped",
                      RoutingKey(msg.platform, msg.bot_id, msg.user_id),
                  )
                  continue
              pool = self.get_or_create_pool(binding.pool_id, binding.agent_name)
              async with pool.lock:
                  # agent.process() deferred to Slice 2+; placeholder for now
                  pass
          finally:
              self.bus.task_done()
  ```
- **Verify:** `pytest tests/core/test_hub.py::TestUnmatchedRouting -x` → GREEN
- **Spec trace:** SC-5, SC-6
- **Phase:** GREEN | **Difficulty:** 3 | **[P] parallel-safe: N**

---

**T18** — Update `src/lyra/core/__init__.py` exports
- **File:** `src/lyra/core/__init__.py`
- **Snippet:**
  ```python
  from .message import (
      ...,
      DiscordContext,
      Platform,
      PlatformContext,
      TelegramContext,
  )
  from .hub import ChannelAdapter, Hub, RoutingKey
  ```
- **Verify:** `python -c "from lyra.core import Platform, TelegramContext, DiscordContext, RoutingKey"` → OK
- **Spec trace:** SC-1, SC-2
- **Phase:** GREEN | **Difficulty:** 1 | **[P] parallel-safe: N**

---

### RED-GATE ✓ — `pytest tests/` must be fully GREEN before proceeding to REFACTOR.

---

### Phase REFACTOR

---

**T19** — Remove `BindingKey` entirely from `hub.py` and `__init__.py`
- **File:** `src/lyra/core/hub.py`, `src/lyra/core/__init__.py`
- **Action:** Delete `BindingKey` class definition. Remove from `__all__` if present. Search for any remaining references.
- **Verify:** `grep -r "BindingKey" src/ tests/` → zero results. `pytest tests/` → GREEN.
- **Spec trace:** SC-1 (zero tests deleted, BindingKey replaced not aliased)
- **Phase:** REFACTOR | **Difficulty:** 1 | **[P] parallel-safe: N**

---

### Final Gate

```bash
pytest tests/ -v          # all tests green
python -m pyright src/    # no type errors on new fields
ruff check src/ tests/    # no lint issues
```

All 6 SC verified → slice complete.
