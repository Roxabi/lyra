.env
.env.*
.env.bak*
# Quadlet secret-reference fragments (bot tokens) — generated by
# `make quadlet-bot-secrets-render`, must never be committed (deploy-host hygiene)
deploy/quadlet/.bot-secrets.*
local/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
coverage.xml
.coverage
.venv/
*.sqlite
*.sqlite-journal
*.db
*.db-shm
*.db-wal
.ruff_cache/
.pytest_cache/
.claude/worktrees/
.claude/*.lock
src/factory/agents/lyra_runtime.toml
factory.toml
config.toml

node_modules/
apps/dashboard/dist/

# Pipeline trace output (generated by integration tests, not versioned)
tests/data/traces/*.json

# Machine-local build state (image digest capture, ADR-053 §3)
.image-digest

# Scratch output from tools/classify_quality_debt.py — regenerate locally, do not commit
artifacts/quality-debt-drain-queue.json

# CocoIndex Code (ccc)
/.cocoindex_code/

# CI junit reports (pytest --junitxml=reports/…, uploaded as artifacts)
reports/
