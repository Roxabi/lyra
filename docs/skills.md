# Skills — How to write a skill

A skill is a sandboxed capability that an agent can invoke — web search, file operations, calculations, external API calls, etc. Each skill declares its permissions explicitly.

<!-- TODO: write this doc after the first skill is implemented -->

## Structure

<!-- TODO: document the SKILL.md manifest format:
  - name, version, description
  - permissions: list of capabilities the skill requires
  - dependencies: other skills or system packages
-->

## Sandboxing

<!-- TODO: document the sandbox constraints:
  - limited environment variables
  - restricted filesystem access
  - network whitelist
-->

## Registration

<!-- TODO: document how the hub builds the skill registry at startup -->

## Example

<!-- TODO: add a minimal skill example (e.g. a calculator or weather lookup) once the skill system is implemented -->
