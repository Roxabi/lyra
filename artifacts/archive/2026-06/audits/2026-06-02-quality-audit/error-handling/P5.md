### Summary
Partition P5 (Adapters, agent_cmd) is clean on bare-except, swallowed-error, and missing-context patterns. One low-severity finding: a broad metadata-parse catch in `jetstream_audio_envelope.py` silently returns a safe default without logging, which could mask JetStream SDK or wire-format regressions.

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P3 | src/lyra/adapters/nats/jetstream_audio_envelope.py | 108 | `except Exception:` swallows metadata parse failures silently; returns safe default `1` without logging, hiding potential JetStream SDK or wire-format issues. |

### Metrics
- files_audited: 58
- issues_found: 1
- P0: 0 | P1: 0 | P2: 0 | P3: 1

### Recommendations (prioritized, with effort estimate)
1. **Add warning log before safe-default return** — `src/lyra/adapters/nats/jetstream_audio_envelope.py:108`: insert `log.warning(...)` before `return 1` in `num_delivered()` so metadata parse failures are visible in production logs. Effort: XS (1 line).
