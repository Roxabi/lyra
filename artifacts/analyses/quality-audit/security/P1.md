### Summary
Partition P1 (Hub, lifecycle, ports) is clean of critical security patterns: no eval/exec, subprocess, pickle, yaml.load, hardcoded secrets, or dynamic imports found. One low-severity finding: `json.loads` on LLM output without size/depth guards in concept/preference extraction.

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P3 | src/lyra/core/lifecycle/session_lifecycle.py | 128 | `json.loads(raw)` parses LLM output from `_extraction_llm_call()` without size/depth limits. Potential DoS via deeply nested or oversized JSON. Exception is caught broadly, but parsing itself may exhaust memory/stack before catch. |
| P3 | src/lyra/core/lifecycle/session_lifecycle.py | 166 | Same pattern as line 128 — `json.loads(raw)` on LLM output for preference extraction. |

### Positive controls observed
| file | line | description |
|------|------|-------------|
| src/lyra/core/hub/middleware/middleware_stt.py | 153 | Slash-command injection guard blocks STT transcripts starting with `/` from entering the command pipeline. |
| src/lyra/core/hub/hub_protocol.py | 52-58 | `ChannelAdapter` protocol documents security contract: adapters must verify sender identity before constructing `InboundMessage`; hub trusts `user_id` and `scope_id` as authenticated. |
| src/lyra/core/ports/blobstore.py | 68-69 | `BlobStorePort` docs reference server-side path containment (`FsBlobStore._safe_resolve_in_root`) yielding 404 on directory traversal attempts. |

### Metrics
- files_audited: 44
- issues_found: 2
- P0: 0 | P1: 0 | P2: 0 | P3: 2

### Recommendations
| priority | recommendation | effort |
|---|-------------|------|
| P3 | Bound `json.loads` depth in `session_lifecycle.py` by passing a custom `object_hook` or using a streaming parser (e.g., `ijson`) to cap nesting depth. Alternatively, pre-truncate `raw` to a reasonable byte limit before parsing. | 1h |
| P3 | Consider adding a `max_size` parameter to `_extraction_llm_call()` or wrapping `json.loads` in a size-limited helper to prevent memory exhaustion from malicious LLM responses. | 30m |
