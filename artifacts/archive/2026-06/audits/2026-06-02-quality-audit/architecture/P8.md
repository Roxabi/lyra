### Summary
P8 partition contains 20 architecture issues across 70 files: one active inbound→adapters layer violation, 6 private-module imports from `lyra.transport._result`, tight encapsulation breaks in `llm_client` and `blobstore`, NxM duplication in LLM codecs, and several known transitional debts still in code.

### Findings
| severity | file | line | description |
|----------|------|------|-------------|
| P1 | src/lyra/inbound/dispatcher.py | 8 | `from lyra.adapters.shared._shared import PushGuardDeps, push_to_hub_guarded` — runtime import from forbidden `adapters` layer into `inbound` stage axis. `.importlister` ignores it as `DEBT:inbound-adapters-transition`, but it remains active code. |
| P1 | src/lyra/llm/cli_nats_codec.py | 22 | `from lyra.transport._result import Err, Result, SanitizedError` — imports private `_result` module. `lyra.transport` public API (`__init__.py`) only exports `SanitizedError`. Couples `llm` to transport internals. |
| P1 | src/lyra/llm/cli_pool_codec.py | 24 | `from lyra.transport._result import Err, Result, SanitizedError` — same private-module import as above. |
| P1 | src/lyra/llm/llm_client.py | 20 | `from lyra.transport._result import Ok` — same private-module import as above. |
| P1 | src/lyra/outbound/error_handler.py | 15 | `from lyra.transport._result import Err, Ok, Result, SanitizedError` — same private-module import as above. |
| P1 | src/lyra/outbound/emitter.py | 40 | `from lyra.transport._result import Err` — same private-module import as above. |
| P1 | src/lyra/outbound/_placeholder_lifecycle.py | 15 | `from lyra.transport._result import Err` — same private-module import as above. |
| P2 | src/lyra/llm/llm_client.py | 156, 186, 209 | `self._pool._transport.call(...)` — direct access to private `_transport` attribute of `WorkerPoolClient`. Acknowledged by `# noqa: SLF001`. Breaks transport-layer encapsulation. |
| P2 | src/lyra/agents/simple_agent.py | 129-130 | Deferred imports `VaultCli` and `WebIntelScraper` inside `_register_session_commands` fallback path. Agent constructs concrete integration implementations locally, violating DI and leaking cross-cutting concerns. |
| P2 | src/lyra/llm/cli_nats_codec.py | 98-128 | `decode`/`decode_chunk` share near-identical error-handling scaffolding with `cli_pool_codec.py` (Err check → JSON validation → fallback LlmResult). NxM duplication. |
| P2 | src/lyra/llm/cli_pool_codec.py | 98-174 | Same duplicated scaffolding as above. |
| P3 | src/lyra/outbound/_streaming_state.py | 128 | Deferred import of `OutboundErrorHandler` with comment claiming circular load with `emitter.py`. `error_handler.py` does not import `_streaming_state.py` or `emitter.py`; no cycle exists. Comment is misleading. |
| P3 | src/lyra/llm/codec.py | 14 | `TYPE_CHECKING` import `from lyra.transport._result import Result, SanitizedError` couples the `LlmCodec` Protocol definition to transport private types. |
| P3 | src/lyra/llm/llm_codec.py | 6 | `from lyra.llm.cli_nats_codec import CliNatsCodec as LlmCodec` — alias collides with the `LlmCodec` Protocol name in `llm/codec.py`, creating naming confusion. |
| P3 | src/lyra/outbound/error_handler.py | 93 | Deferred import `from lyra.core.exceptions import StreamChunkTimeout` inside `classify_stream_error`. No cyclic dependency exists; unnecessary local import. |
| P3 | src/lyra/inbound/context.py | 31-32 | `TYPE_CHECKING` imports `TypingTaskManager` and `OutboundListener` from `lyra.adapters`. Known debt (`DEBT:inbound-adapters-transition`), still present. |
| P3 | src/lyra/inbound/wire_parser_telegram.py | 10 | `TYPE_CHECKING` import `from lyra.adapters.telegram.telegram import TelegramAdapter`. Known debt (`DEBT:inbound-adapters-wireparser`). |
| P3 | src/lyra/inbound/wire_parser_discord.py | 15 | `TYPE_CHECKING` import `from lyra.adapters.discord.adapter import DiscordAdapter`. Known debt (`DEBT:inbound-adapters-wireparser`). |
| P3 | src/lyra/agents/simple_agent.py | 25 | Re-exports `STTNoiseError` from `lyra.core.ports.stt` — adds an extra dependency layer not needed for the agent contract. |
| P3 | src/lyra/blobstore/_handlers.py | 81 | `store._conn` — direct private-attribute access on `FsBlobStore` (external package). Breaks encapsulation. |

### Metrics
- files_audited: 70
- issues_found: 20
- P0: 0 | P1: 7 | P2: 4 | P3: 9

### Recommendations
1. **Public re-export of `Result`/`Ok`/`Err` from `lyra.transport`** (effort: small) — update `transport/__init__.py` to export `Result`, `Ok`, `Err` alongside `SanitizedError`, then migrate all 6 P1 imports to `from lyra.transport import ...`.
2. **Relocate `push_to_hub_guarded` and `PushGuardDeps` to `lyra.core`** (effort: medium) — follow #1283 Phase 6 to eliminate the active inbound→adapters runtime dependency.
3. **Add public `call` method to `WorkerPoolClient`** (effort: small) — remove the `SLF001` violation in `llm_client.py` by giving the pool a public control-plane API.
4. **Extract shared LLM codec error-handling helper** (effort: medium) — create a private `llm/_codec_common.py` helper for the duplicated Err→validation→fallback path shared by `CliNatsCodec` and `CliPoolCodec`.
5. **Remove `SimpleAgent` fallback construction of `SessionTools`** (effort: small) — make `session_tools` a required constructor argument; delete the local `VaultCli`/`WebIntelScraper` fallback.
6. **Clean up misleading deferred imports and `_conn` accesses** (effort: small) — remove unnecessary deferred imports in `_streaming_state.py` and `error_handler.py`; either request a public `conn` accessor from `roxabi_blobs` or wrap the store in `blobstore`.
