# Task 0001 — _Short title_

> Replace this file with one concrete task.
> Each task file covers exactly one logical unit of work (one function, one file, one migration).
> The agent must complete this task fully before opening the next file.

## Context

_Why this task exists. What problem it solves. What the agent needs to know before starting._

Reference files the agent should read before starting:
- `src/lyra/core/message.py` — see the `Platform` enum pattern
- `tests/core/conftest.py` lines 12–40 — fixture conventions

## Action

Precise, imperative steps. No ambiguity.

1. Open `src/lyra/<module>/<file>.py`.
2. Add the following import at the top of the imports block:
   ```python
   from lyra.<module>.<submodule> import <ClassName>
   ```
3. Add method `foo()` to class `Bar` with this exact signature:
   ```python
   async def foo(self, pool_id: str) -> <ReturnType>:
       ...
   ```
4. Implement the body: _describe the logic in plain English, not pseudocode_.

## Files changed

| File | Change type |
|------|-------------|
| `src/lyra/<module>/<file>.py` | modify |
| `tests/<module>/test_<file>.py` | modify |

## Expected outcome

After this task completes:

```bash
uv run python -c "from lyra.<module>.<file> import Bar; assert hasattr(Bar, 'foo')"
# → no error
```

```bash
uv run pytest tests/<module>/test_<file>.py::TestBar::test_foo -v
# → 1 passed
```

## Commit

```
feat(<scope>): add foo() to Bar
```

---

_Next task: `task-0002.md`_
