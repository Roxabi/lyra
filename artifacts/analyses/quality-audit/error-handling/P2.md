### Summary
Partition P2 (Agent model, CLI, commands) has 8 medium and 5 low severity error-handling issues. No critical or high severity issues found. The main patterns are: swallowed errors without logging (3 cases), overly broad `except Exception` catches with insufficient log levels or missing tracebacks (5 cases), and minor consistency/robustness gaps (5 cases).

### Findings
| severity | file | line | description |
|---|------|------|-------------|
| P2 | src/lyra/core/agent/agent_refiner_stages.py | 43 | Swallowed `json.JSONDecodeError`/`TypeError` with `pass` — persona_json parse failure in `build_system_prompt` produces no log and omits profile data from the LLM prompt silently |
| P2 | src/lyra/core/agent/agent_refiner_stages.py | 48 | Swallowed `json.JSONDecodeError`/`TypeError` with `pass` — voice_json parse failure in `build_system_prompt` produces no log and omits voice data from the LLM prompt silently |
| P2 | src/lyra/core/agent/agent.py | 124 | Broad `except Exception` at DB boundary (`_agent_store.get`) logs at `debug` level only — production deployments at INFO level will never see DB unavailability |
| P2 | src/lyra/core/agent/agent.py | 145 | Broad `except Exception as exc` at config reload boundary logs `warning` without `exc_info=True` — traceback lost when `agent_row_to_config` fails |
| P2 | src/lyra/core/cli/cli_pool_entry.py | 51 | Broad `except Exception` for session-persist callback failure logs at `debug` level — session loss on restart is invisible at default log levels |
| P2 | src/lyra/core/cli/cli_pool_worker.py | 136 | Broad `except Exception as exc` at idle-reaper top-level boundary logs `warning` without `exc_info=True` — traceback lost when reaper crashes |
| P2 | src/lyra/core/commands/command_router.py | 127 | Broad `except Exception as exc` for processor command loading logs at `debug` level — incomplete command listings invisible at default log levels |
| P2 | src/lyra/core/commands/builtin_commands.py | 90 | Broad `except Exception as exc` for processor description loading logs at `debug` level — incomplete help output invisible at default log levels |
| P3 | src/lyra/core/agent/agent_commands.py | 76 | `except OSError: pass` in `_record_command_mtimes` — mtime read failure silently skipped, no log to indicate hot-reload detection may be delayed |
| P3 | src/lyra/core/agent/agent_db_loader.py | 46-163 | 10 `json.loads()` calls on DB JSON columns without try/except — malformed DB JSON propagates as `json.JSONDecodeError` to callers (unlike `bot_models.py` which guards and logs) |
| P3 | src/lyra/core/agent/agent_refiner.py | 227, 247 | `driver.chat()` calls in `run_session` lack try/except — LLM/network failure crashes the interactive refinement session instead of returning a graceful error |
| P3 | src/lyra/core/commands/command_router.py | 315 | Catches `TimeoutError` instead of explicit `asyncio.TimeoutError` — works in Python 3.12 but inconsistent with rest of codebase (line 286 uses `asyncio.TimeoutError`) |
| P3 | src/lyra/core/cli/cli_pool_session.py | 66 | `except RuntimeError: pass` for "no running loop" is too broad — could mask unrelated `RuntimeError` from `publisher.publish_set_cli_session()` |

### Metrics
- files_audited: 37
- issues_found: 13
- P0: 0 | P1: 0 | P2: 8 | P3: 5

### Recommendations (prioritized, with effort estimate)

1. **Add logging to swallowed JSON errors** (P2, ~10 min) — In `agent_refiner_stages.py` lines 43-44 and 48-49, replace `pass` with `log.warning("...", exc_info=True)` so malformed persona/voice JSON is visible.

2. **Elevate log level for DB boundary failures** (P2, ~10 min) — In `agent.py` line 124, change `log.debug` to `log.warning` and add `exc_info=True` so DB unavailability is visible in production logs.

3. **Add `exc_info=True` to broad exception handlers** (P2, ~15 min) — In `agent.py:145`, `cli_pool_worker.py:136`, and `cli_pool_entry.py:51`, add `exc_info=True` (or `log.exception`) to preserve tracebacks for debugging.

4. **Elevate processor loading log levels** (P2, ~10 min) — In `command_router.py:127` and `builtin_commands.py:90`, change `log.debug` to `log.warning` so command listing gaps are visible.

5. **Add defensive JSON parsing in `agent_db_loader.py`** (P3, ~20 min) — Wrap `json.loads()` calls in try/except with logging and fallback defaults, matching the pattern in `bot_models.py`.

6. **Add LLM failure handling in `agent_refiner.py`** (P3, ~15 min) — Wrap `driver.chat()` calls in `run_session` with try/except to return a graceful error Response instead of crashing the session.

7. **Standardize timeout exception type** (P3, ~5 min) — In `command_router.py:315`, change `except TimeoutError` to `except asyncio.TimeoutError` for consistency.

8. **Narrow RuntimeError catch in `cli_pool_session.py`** (P3, ~5 min) — Check `str(exc)` for "no running loop" before swallowing, or use a more specific check for the test-context case.
