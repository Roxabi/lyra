# roxabi-obs

Fleet plane ③ reporter only — periodic `ContainerReport` publish to
`factory.metric.host.container_report`.

Not the OTel/Langfuse trace stack (`src/factory/obs/`). No `factory.*` imports.
Deps: `roxabi-contracts`, `roxabi-nats`.