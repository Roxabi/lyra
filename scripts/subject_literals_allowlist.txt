# subject_literals_allowlist.txt — INTENTIONALLY EMPTY (gate is fully strict).
#
# This allowlist baselined pre-existing orphan NATS subjects so
# check_subject_literals.py could ship green. As of #1545 it is retired: both
# remaining entries were declared at their source and removed from here, so the
# gate now requires every raw lyra.* subject literal in src/ to resolve — no
# baselined debt remains.
#
#   - lyra.gh.mint_failure.>  → hub subscribe grant in deploy/nats/acl-matrix.json
#   - lyra.verify.deny        → roxabi_contracts.verify.SUBJECTS (reserved sentinel)
#
# DO NOT add entries here. The correct fix for a new orphan subject is to declare
# it in acl-matrix.json (publish/subscribe grant) or in roxabi-contracts SUBJECTS.
# Keeping this file empty (rather than deleting it) documents that the gate is
# strict by design and guards against silent re-introduction of baselined debt.
